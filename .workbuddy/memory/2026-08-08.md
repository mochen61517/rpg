# 2026-08-08 RPG 工作日志

## 改动：今日一句（明信片）背景改书信质感
- 用户反馈：不要单一色块，要"背景有书信质感，像是来信那种"。
- 改 `.panel.quote-postcard`（888 行）背景：单一暖色渐变 → 多重背景叠层 = 信笺横线（`repeating-linear-gradient` 180deg 每 27px 细横线）+ 纸张做旧斑驳（3 个不规则 radial-gradient 淡棕斑）+ 极淡纤维纹理（48deg repeating）+ 暗角（inset box-shadow）+ 暖纸底渐变。不再是单一色块。
- `.pc-main`（891 行）加左侧淡红页边线 `border-left:2px solid rgba(168,68,52,.28)`（信笺左边距红线感）。
- `.pc-side`（897 行）背景 `rgba(255,255,255,.30)` → `transparent`，让右侧信纸纹理透出（邮票自身白底保留）。
- 邮票/邮戳/换一句按钮布局不变。
- 验证：`<style>` 块 `{`/`}` 平衡（883/883），改动 grep 命中。纯 CSS 改动，未跑 JS 运行时。
- 仅改主文件 `life-rpg.html`，**未同步 deploy/**（deploy 仍停在 8-06 的 commit 0706e67，本次改动未包含）。

## 改动：明信片去掉右侧猫咪邮票
- 用户："右侧明信片的邮票那里不需要。"
- 删 CSS `.pc-stamp` 全部 4 条规则（原 907-910 行：邮票方块 / face / cap / ::after 齿孔虚线）。
- 删 `renderQuote()`（~5105 行）中的 `<div class="pc-stamp">🐱 修行录</div>` 拼接行。
- `.pc-side` 宽 118px→104px，padding 改 `18px 12px 14px`；`.pc-postmark` 由 `position:absolute; right:6px; top:46px`（原压在邮票下方）改为 `position:relative` 随 flex 流居中靠上。日期邮戳 + 「换一句」按钮保留。
- 验证：`<style>` `{`/`}` 平衡 879/879（= 原 883 − 删掉的 4 对），`pc-stamp` 全文 0 残留，`pc-inner/pc-main/pc-divider/pc-side` 标签配平。
- 仍**未同步 deploy/**（deploy 已落后主文件 3 次改动：书信质感、去邮票）。

## 改动：疗愈类任务同类互斥 + 21天冷却修复（v5.21.1）
- 用户反馈："随机任务和周任务有按摩和理疗，不应该同时出现。"
- **根因（三条，均已验证）**：
  1. 随机周游池 W（2123-2126）有 4 条疗愈类（按摩/康复理疗/头疗/眼部护理），`sampleFromBank('weekly',3)` **无同类去重** → 同批可撞车。
  2. 老存档 `S.weekly` 残留旧固定周常 `完成 1 次体检/理疗/体态评估`（deploy `0ac30d3:index.html` 2128 行确认）。8-06 整合只清了 defaultState 种子，老存档保留 → 变"手动周目标"每周常驻，与随机抽的按摩/理疗撞车。
  3. **真 bug**：21 天间隔的 `lfDates` 挂在**任务对象**上，而 `sampleFromBank` 每周一生成全新对象、不继承 → 冷却跨周完全失效，`⏳冷却中` 也不显示。
- **修法**：冷却从「对象级」升为「组级」。
  - 新增 `LF_GROUPS`：`heal`（头疗|按摩|康复|理疗|推拿|体态评估，21天）/ `eye`（眼部护理，10天）。
  - 新增 `S.lfLog={heal:[],eye:[]}` 组日志（落盘，跨对象跨周持久）+ `lfGroupOf/lfLogOf/lfGroupDaysLeft/lfHealedWithin/lastHealDate`；`lastLfDate` 改为组日志 ∪ 对象 lfDates（兼容老存档）。
  - `sampleFromBank` 三重互斥：手动周目标/日任务已有同组 → 不抽；组冷却未过 → 不抽；同批同组最多 1 条。加 `if(!avail.length) break` 防死循环。
  - `toggle()` 完成时写组日志、**取消勾选时撤销**（旧代码取消勾选不撤销 lfDates，一并修了）。
  - `energyStateForDate`/`isHealedOn`/`energyAdvice` 三处改读组日志（原来遍历 allTaskLists 读对象 lfDates，随机项重抽后必然失真）。
  - `migrate()` 一次性迁移 `migLf211`：散落各列表（含 year.items）的 lfDates 并入组日志 → 再删旧种子（用户在 AskUserQuestion 中选「删掉它」）。
- **坑**：`defaultState()` 里 `migLf211` 必须为 `false`。`load()` 用 `Object.assign(defaultState(), 老存档)`，若默认 true 会覆盖老存档缺失值 → 迁移永不执行。已注释标注。
- **验证（node 跑真实代码块）**：互斥 7 项全过（500 轮同批 heal>1 次数=0、抽到 heal 228 批未误杀、手动已有理疗时泄漏 0、冷却中泄漏 0 且 eye 不受影响、冷却过后恢复 148 批、全新理疗对象 lfDaysLeft=14、每批仍满 3/4/2 条）；迁移 4 项全过（旧种子删除且其他手动项保留、历史正确并入、冷却算对、幂等、新用户无副作用）。全文件内联 script 语法 0 错误、style 花括号 879/879、旧实现 0 残留。
- 仍**未同步 deploy/**。

## 改动：dashboard 布局重排（动作优先）
- 用户："现在很多设计都是单独的区块，然后平铺排列。看看有没有更合理，更舒服的排列方式"。
- 诊断（基于代码）：① 顺序埋没重点——今日行动/本周任务/精力原排在第 6/8/9 位，首屏被「今日一句→通知→抽签→角色卡→总等级→年龄」7 个整宽块压住；② 列宽空档——daily(col4)+weekly(col4)=8 列，ledger(col6) 换行留 4 列空档；③ 无层次。
- 方案（用户选"动作优先·重排"，保留拖拽排序）：只调顺序+列宽，不动 JS 逻辑。
  - 新顺序（dom 即默认序，drag-sort 无 localStorage 时回退到此）：quote → notif → **daily(col6) / weekly(col6)** → **energy(col12)** → **journey(col4) / level(col4) / bioAge(col4)** → ledger(col6)/badges(col6) → month(col6)/year(col6) → catstatus(col12) → trend(col12) → **draw(col12, 沉底)**。
  - 关键列宽变更：daily/weekly col4→col6；hero 加 col4；level/bioAge col12→col4；draw 从顶部第 3 位移到底部。
  - 实现：顶部把 draw 块删除、将 daily/weekly/energy 三块插入到 hero 之前（hero 加 col4）；下方大区块删除原 energy/daily/weekly、level/bioAge 改 col4、draw 沉底。**刻意不碰 role卡内嵌 base64 头像**（避免无法精确匹配），靠插入/删除移动而非重写。
- 验证（node）：dash-grid 内 data-dash-id 顺序与预期完全一致、13 张无重复、每行列宽和=12 无孤儿行；`<style>` 879/879 平衡（CSS 未改）。
- deploy 已落后主文件 **4 次**（书信质感、去邮票、疗愈修复、本次重排）；之前提过的 deploy 分支与 origin/main(f5eb2e8) 分叉、force push 仍待用户拍板。

## 改动：合并「修行树」与「徽章成就」为「修行·成长」页（v5.21.2）
- 用户："徽章成就、修行树和本来升级进度条的部分有些重复。有什么好的想法？"
- 分析（基于代码）：总等级(dashboard level 卡+renderLevelBar)、修行树(page-skill: SKILL_TREE 四系各6节点, 用修行点解锁给该系经验加成)、徽章成就(page-badges: S.ach 全局里程碑+羁绊+愿望+爱好) 三块都挂"四系成长"但三套独立机制，用户要在三处拼"我成长到哪了"。
  - 关键事实：天赋树/羁绊/赛季称号都天然按四系(BADMINTON/CAREER/BODY/MIND)分组；但 **S.ach 成就无系字段**（全局里程碑：灯火不熄/立基上岸/云水初临等），强行分系会牵强且要动存档。
- 方案（用户选"统一修行页"）：新建 `page-growth`，收拢原 page-skill + page-badges 的全部子容器 id（skillBox/seasonBox/achsUnlocked/achsLocked/bondBox/wishGroups/hobbyList）+ 顶部加成长页专属等级条(growthLvl/growthXpFill/growthXpText/growthStreak/growthPoints)。
  - 诚实调整：四系卡同框展示"天赋树+羁绊+赛季称号"（三者都分系）；成就因无系字段，作为"全局里程碑"区放在四系下方集中。重复感消除，不破坏已验收的 dashboard 布局（level 卡保留，仅加 onclick 进 growth）。
  - nav 的 skill+badges 两入口合并为一个 `growth`（🌳 修行·成长），navBadge-growth 承载羁绊未读提示（原 navBadge-badges）。
  - dashboard badges 卡 onclick→growth（内容保留为"江湖名帖"入口）；level 卡加 onclick→growth。
- 关键技术点：
  - 所有子渲染函数(renderSkillTree/renderSeason/renderBonds + render() 内成就段)写死固定 id、不绑页面 → 只把容器挪到 page-growth 即可，**JS 渲染逻辑基本不动**。
  - 新增 `renderGrowthLevel()`（4804 行）：复用 overallXP()/lvlOf()/xpInLvl()/skillPointsTotal()/skillPointsLeft()/computeStreak()，写到 growth 页专属 id；在 render() 内 `try` 调用（3981 注释后）。
  - 调用点替换：showPage 4384 'badges'→'growth'；navBadgeCount 5464 'badges'→'growth'；renderNavBadges 5468 数组 'badges'→'growth'；restore 5836 列表 'badges','skill'→'growth'。
- 验证：① 内联 script `new Function` 语法 0 错误；② grep 残留页面引用(showPage('badges')/page-badges/navBadge-badges 等) 0；③ page-growth 容器 + 12 个子 id 全存在；④ showPage('growth') 3次/data-page="growth" 1次/navBadge-growth 1次/renderGrowthLevel 定义+调用均存在；⑤ CSS 879/879 平衡；⑥ 聚焦运行时验证(提取真实函数块+构造合法S+mock DOM 调 renderGrowthLevel)：5 个 id 全正确写入(Lv.16 / 69% / 1073·1550 / 修行点13·15 / streak1)。
- **未同步 deploy/**（deploy 已落后主文件 **5 次**：书信质感、去邮票、疗愈修复、布局重排、本次合并）。
