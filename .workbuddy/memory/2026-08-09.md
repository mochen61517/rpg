# 2026-08-09 RPG 工作日志

## RPG 项目（E:\.workbuddy\RPG）结构排查 + 冲突确认
- 源文件在 `assets/`（core-state.js / tasks-render.js / life-rpg.css / systems.js / npc-stories.js；life-rpg.js 是 8/8 旧版未动）。
- 构建层在 `deploy/deploy/assets/`，最终产物 `deploy/index.html`（22:36 生成）。根 `life-rpg.html` 是 8/8 单文件旧版。
- 兴趣爱好模块（截图里"平时兴趣爱好"标签）分布：
  - HTML：`page-growth` 页 `id="hobbyList"` 的 `<h2>🎯 平时兴趣爱好</h2>` 面板（含江湖名帖 hint）
  - JS：`core-state.js` 的 `defaultHobbies()` + `S.hobbies` 默认；`tasks-render.js` 的 `renderHobbies()` / `toggleHobby()` / `render()` 中 `hobActive` 引用
  - CSS：`life-rpg.css` 约 519-523 行 `.hobby` 样式块
- **冲突信号**：v5.44 改版脚本 `_edit_544.py` 22:33 在改 `deploy/deploy/assets/*.js`；「人生RPG 每日22点晚间复盘推送」自动化 ACTIVE（22:00 刚跑）；系统有 node/python 进程在跑。
- **用户决策（2026-08-09 22:40）**：选「先暂停，任务跑完再改」——不修改任何文件，等用户确认 v5.44 + 复盘任务都结束后，再整体移除兴趣爱好模块。
- **已执行**：v5.44 任务结束后（deploy/deploy/assets mtime 22:33 稳定），在 `life-rpg.html` 的 `page-growth` 区域移除了 5 个 panel：
  1. `skillBox` 空 panel
  2. `修行卷册` panel（📖 修行卷册 · 四卷）
  3. `待揭帖` panel（🔒 待揭帖）
  4. `bondBox` 空 panel
  5. `平时兴趣爱好` panel（🎯 平时兴趣爱好）
- 修改后备份在 `life-rpg.html.bak`。
- `page-growth` 当前保留：总等级、seasonBox、已扬名、人生愿望。
- `deploy/index.html`（v5.44 产物）中仍保留兴趣爱好 panel 和 bondBox 空 panel；如需要同步，需单独处理。

## RPG 修为账本柱状图 → 折线图
- 定位：`renderXpLedger()` 在 `deploy/assets/tasks-render.js` / `deploy/deploy/assets/tasks-render.js` / `assets/tasks-render.js`。
- 修改：把 `xpl-week` 里的 7 个柱状 `<i>` 替换为 SVG `<polyline>` + `<circle>` 折线/点，保留底部日期标签。
- 同步改了三层 tasks-render.js，并把 `deploy/index.html` 中 `tasks-render.js?v=5.44.1` 升级到 `?v=5.44.2` 以刷新浏览器缓存。
- 三层 JS 均通过 `node --check` 语法校验。

## 2026-08-09 23:00 再次调整：修为账本柱状图 → 折线图
- 用户反馈截图仍显示柱状图。实际排查发现：
  - `assets/tasks-render.js` 之前存在 bug：`const ...; +'<div...'` 导致折线图 HTML 没被拼接到 `innerHTML`，所以旧版柱状图仍显示（或该文件被覆盖）。
  - `deploy/assets/tasks-render.js` 与 `deploy/deploy/assets/tasks-render.js` 已经是折线图代码，但日期标签布局还是每个日期单独 `.xpl-day` div，与新 CSS 不匹配。
- 已修改：
  - `assets/tasks-render.js`：修复并统一为折线图 + 单行日期标签布局。
  - `assets/life-rpg.js`：同步从柱状图改为折线图。
  - `assets/life-rpg.css`、三层 `life-rpg.css`：`.xpl-week`/`.xpl-day`/`.xpl-chart` 样式改为适配折线图。
  - `deploy/assets/tasks-render.js`、`deploy/deploy/assets/tasks-render.js`：日期标签改为单行布局。
  - `deploy/index.html`：CSS 版本号从 `?v=5.44.1` 提升到 `?v=5.44.2`。
  - `life-rpg.html`：CSS/JS 版本号从 `?v=5.34.0` 提升到 `?v=5.34.1`。
- 注意：`deploy/deploy/assets/` 在 23:12 仍被 v5.44 脚本覆盖，该层修改可能回退；`deploy/assets/` 与 `assets/` 相对稳定。

## 2026-08-09 23:18 同步清除 deploy/index.html 残留 panel
- 用户确认"都清掉"：将 deploy/index.html 中残留的 bondBox 空 panel 与 平时兴趣爱好 panel 一并删除，使两个入口（life-rpg.html / deploy/index.html）清理范围一致。
- 动手前核对：v5.44 脚本已停（进程数 0，_edit_544.py mtime 停在 22:33 未再变）；deploy/index.html 内联 JS 无 hobbyList/bondBox 的 getElementById 引用，也无 renderHobbies/defaultHobbies/toggleHobby/S.hobbies，删 HTML 不会 null 报错。
- 删除后 deploy/index.html 的 page-growth 仅剩：总等级、已扬名、人生愿望。待揭帖/修行卷册/赛季称号在 v5.44 构建时已是注释移除。
- 两个入口现已一致；备份 deploy/index.html.bak 已删。

## 2026-08-10 记忆路径纠正
- 发现：8/9 工作日志最初误写到会话工作区 `E:\.workbuddy\2026-08-09-22-39-08\.workbuddy\memory\2026-08-09.md`，但 RPG 项目自身的记忆目录是 `E:\.workbuddy\RPG\.workbuddy\memory\`（7/29–8/8 日志均在此）。会话工作区目录随会话窗口消失，跨会话新对话读不到。
- 已纠正：将 8/9 日志迁至 `E:\.workbuddy\RPG\.workbuddy\memory\2026-08-09.md`，并删除会话工作区那份。
- 约定（已写入 cross-session-iteration skill）：RPG 项目记忆一律写 `E:\.workbuddy\RPG\.workbuddy\memory\YYYY-MM-DD.md`，新对话第一句让用户指定该路径恢复上下文。
