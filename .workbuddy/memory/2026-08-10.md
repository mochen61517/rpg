# 2026-08-10 RPG 工作日志

## 恢复上下文 + 永久移除兴趣爱好 panel（构建源修复）
- 读 `2026-08-09.md` 恢复上下文：8/9 曾在 `life-rpg.html`（5.34.1）移除 5 个 panel（skillBox/修行卷册/待揭帖/bondBox/平时兴趣爱好），并清过 `deploy/index.html`，但 `deploy/` 是 `_edit_544.py`/`_fix_*.py` 的**就地改发布层**（`_edit_544.py` BASE=`deploy/deploy`，从 `deploy/deploy/index.html` 读写）。
- **今晨实测回归**：`deploy/index.html` mtime 09:44、版本 v5.45.2，被一次 v5.45 构建重新生成，把 8/9 清掉的 panel 又带回来了。日志当初对"部署层会被构建覆盖"的警告应验。
- **架构澄清（重要）**：项目有三套并行副本——
  1. 根单文件 `life-rpg.html`（已干净，无 hobby 代码）
  2. 根部署 `deploy/index.html` + `deploy/assets/*.js`（运行时加载 `deploy/assets/`）
  3. 嵌套发布真源 `deploy/deploy/index.html` + `deploy/deploy/assets/*.js`（Actions 发布这个；`_edit_544.py` 读写此层）
  `assets/`（开发源）与 `_edit_544.py` 的 BASE 不一致——`_edit_544.py` 不碰 `assets/`，改的是 `deploy/deploy/`。
- **panel 真实分布（发布层实测）**：`deploy/deploy/index.html` 与 `deploy/index.html` 里 修行卷册/待揭帖 早已是"已移除"注释；真正残留可见的 panel 只有 **bondBox 空 panel** 和 **平时兴趣爱好 panel（#hobbyList）**。
- **永久修复**：写 `_fix_panels.py`，用正则从两个 index.html 删除 bondBox panel + 平时兴趣爱好 panel（含外层 `<div class="panel">`）。已运行，两个入口均删除成功。
- **验证**：① 两入口 `hobbyList`/`bondBox` 的 `id=` 已查无；② `renderHobbies()` 仍有 `if(!el) return` 守卫，删 HTML 不报错；③ grep 全部 `_*.py`，无任何构建脚本引用 hobbyList/平时兴趣爱好/bondBox/page-growth/renderHobbies → 后续构建不会复活（永久成立）；④ 两入口 page-growth 结构一致（总等级 / 已扬名 / 人生愿望 + 历史"已移除"注释）。
- **遗留（未动，可选）**：JS 里 `S.hobbies` 默认数据 + 仪表盘徽章卡"兴趣"小标签（render 内 hobActive，tasks-render.js 约 1833-1835/1911/1919 与 2006/2034/2065 区段；嵌套版 tasks-render.js 该段**疑似重复出现两次**，直接改 JS 风险较高）仍在。表现为"江湖名帖"摘要里仍显示默认兴趣，无害、已被守卫。如需与 life-rpg.html（零 hobby 代码）完全对齐，需另做一趟谨慎的 JS 清理（建议先 node --check 每张改动的 JS）。
- 规则沿用：RPG 记忆一律写 `E:\.workbuddy\RPG\.workbuddy\memory\YYYY-MM-DD.md`。

## 11:38 三套副本整体统一到 deploy/deploy 完整目标态（根治"不同步"根因）
- **用户需求**：本周行动→江湖周榜；加江湖月榜（同逻辑：难度高/积分高在上）；手动周目标去掉；周报月报放最底。
- **关键发现（纠正上午判断）**：`life-rpg.html`（778-781 行）引用相对 `assets/*.js?v=5.34.1` → 它加载的是**开发源 `assets/`**，而 `assets/` 比 `deploy/deploy/` 严重落后（连江湖日榜都没有，还在用旧 `S.mainQ` 字段）。`deploy/deploy/` 才是 `_edit_544.py` 等脚本就地改的**发布真源 + 事实上的完整版**（含江湖日榜+周榜+月榜+今日主线）。此前那次"江湖周榜/月榜"改动只在 `deploy/deploy` 做了，从不回流 `assets/`，所以用户打开 `life-rpg.html` 看到的还是旧"本周行动"。
- **函数级差异分析（覆盖安全性）**：`core-state.js`/`npc-stories.js` 两版完全相同；`systems.js` 是 `deploy/deploy` 超集（多 8 函数）；`tasks-render.js` 中 `assets/` 独有的 28 个定义全是**被江湖系统取代的废弃旧功能**（FATE_NAMES/chooseTodayFate/renderLoadAdvisor/sideDailyList 等）。→ 用 `deploy/deploy/assets/*` 覆盖 `assets/*` 不会丢有用功能。
- **执行（已备份 `.sync_backup_20260810-113855`）**：
  1. 备份 `assets/`(4js+css)、`deploy/assets/`(4js+css)、`life-rpg.html`、`deploy/index.html` 到 `.sync_backup_20260810-113855/`。
  2. 复制 `deploy/deploy/assets/{core-state,tasks-render,npc-stories,systems}.js`+`life-rpg.css` → `assets/` 和 `deploy/assets/`。
  3. 复制 `deploy/deploy/index.html` → `life-rpg.html` 和 `deploy/index.html`（相对 `assets/` 路径解析一致：根 life-rpg.html→`E:/.workbuddy/RPG/assets/`；deploy/index.html→`E:/.workbuddy/RPG/deploy/assets/`）。
- **验证（三套完全一致）**：jianghuWeekBox/jianghuMonthBox 三套均=1；weeklyGoalList 三套均=0（手动周目标已移除）；周报月报位于 page-week 之后、文件最底（行651/总行716）；三套 `jianghuPeriodReroll` 一致；`node --check` tasks-render.js/core-state.js 通过。
- **排序逻辑确认**：`ensureJianghuPeriod` 用 `list.sort((a,b)=>(b.diff-a.diff)||(b.xp-a.xp)||...)`，并按 `[5,4,3,2,1]` 难度分层从 `JIANGHU_BANK` 抽取（周3/月5 条）→ 难度高、积分高在上，与日榜同逻辑。
- **副作用说明**：整体统一后 `life-rpg.html` 不再含旧"赛季称号"面板 `seasonBox`——其对应渲染函数在 `deploy/deploy` 版 JS 中已不存在，统一前本就是空壳，移除后更干净。旧的 `loadAdvisorBox`/`sideDailyList`/`goalsBox` 等废弃面板同理随统一清理。
- **后续注意**：`deploy/deploy/` 仍是被 `_edit_544.py` 等脚本修改的发布层。今后改功能应**同时改 `deploy/deploy/` 并回流 `assets/`（开发源）**，避免再次不同步。本次已把 `assets/` 拉齐到 `deploy/deploy`，但 `_edit_544.py` 下次运行只会再改 `deploy/deploy`，不会动 `assets/`——若后续在 `assets/` 单独改，需重新同步三套。

## 12:0x 顶部双 tab + 江湖榜 + 我的揭榜（交互重构 v5.47.0）
- **用户需求**：今日行动与周月任务合并到顶部 Tab A「今日行动·长期复利」；Tab B「江湖榜」含江湖日/周/月榜 + 我的揭榜；可在榜上"揭榜"认领任务，进入「我的揭榜」看状态/截止日/完成与否；未完成的揭榜进 dashboard 提示区（与未回消息、未兑奖品同区）；手动周目标已在上一步去掉；周报月报放最底。
- **已确认的三处设计决策（用户拍板）**：① 截止日=周期末（日→今日24:00 / 周→本周日24:00 / 月→月末24:00）+ 逾期标红保留、仍可完成但只给基础分、无"准时"加成 → 实现为**准时完成给 base×1.2（含准时加成）、逾期只给 base**；② 我的揭榜=江湖榜页内区块（日/周/月榜下方）；③ 同周期同任务只揭一次（按 src+id+period 去重），完成记一次分、原榜同步完成。
- **实现（先改权威副本 `deploy/deploy/`，再整体同步三套）**：
  - HTML：`nav` 改为「顶部双 tab（`今日行动·长期复利`=action / `江湖榜`=jianghu）+ 其余页常规 nav」；`page-current`→`page-action`（合并今日+周月内容，移除江湖日榜 panel，周报月报置底）；`page-week`→`page-jianghu`（日/周/月榜 + 我的揭榜区）；page-data 移除周报面板（避免与 action 重复）。版本戳 `?v=5.47.0`。
  - JS(tasks-render.js)：新增 `jianghuAccept`(认领+算截止日+去重) / `jianghuMyToggle`(我的揭榜完成/撤销，带准时加成) / `renderMyJianghu`(我的揭榜渲染，未完成在上、逾期标红) / `jianghuDeadline` / `fmtDeadline` / `syncMyJianghu`(原榜完成时同步 myJianghu) / `myJianghuUnfinished`；`renderJianghu`/`renderJianghuPeriod` 每行加「🗡️ 揭榜」按钮（用 `data-src`/`data-id` 属性传参，避开嵌套引号转义坑）；`render()` 调 `renderMyJianghu()`；`showPage` 的 markSideSeen 范围改为 action/jianghu。
  - JS(systems.js)：`notifList` 加未完成揭榜项（逾期优先，链 jianghu 页 myJianghuBox）；`navBadgeCount` 加 jianghu 未完成数；`renderNavBadges` 列表加 action/jianghu；`reorganizeDetailPages` 的 move/setHead 由 current/week 改为 action/jianghu（羽毛球专精→growth、任务生成器→data、月行大计 rename、周报move移除）；init 默认页列表加 action/jianghu。
  - JS(core-state.js)：`defaultState` 加 `myJianghu: []`。
  - CSS：`.top-tabs`/`.tt-main.cur`(金色高亮) + 我的揭榜 `.myjh-overdue/.myjh-done/.jh-dead/.jh-src` 样式。
- **踩坑（重要）**：Python 脚本里生成 JS 的 `onclick="jianghuAccept(\''day\',...)"` 时，`\\''` 会多生成一个未转义引号破坏字符串 → `node --check` 报 SyntaxError。改用 `data-src="day" data-id="'+x.id+'" onclick="jianghuAccept(this.dataset.src,this.dataset.id)"` 彻底规避嵌套引号。改完务必 `node --check` 三套 JS。
- **验证（三套完全一致 + 语法通过）**：page-action/page-jianghu/myJianghuBox/top-tabs/navBadge-jianghu 三套均=1；无 page-current/page-week/navBadge-current 残留；reportWeek 三套均=1（仅在 action 页）；jianghuAccept/renderMyJianghu/myJianghuUnfinished 三套均定义、jianghuAccept(this.dataset.src 均=2（日+周期）、renderMyJianghu() 均=2；systems.js jianghu 徽章+myjianghu 通知均就位，无 setHead('current'/'week')；三套 `node --check` tasks-render/systems/core-state 全过。
- **脚本留存**：`_rpg_refactor.py`（整体重构，含 replace_once 断言，非幂等——重跑会失败，勿重复执行）、`_rpg_page_action.txt`/`_rpg_page_jianghu.txt`（新页面块）。

## 12:2x 江湖委托·对话回应（v5.48.0）
- **用户需求**：江湖委托每张卡应能"回应/对话"（如练了什么曲子、练得怎样），故人据此更了解用户、产生更高质量互动。
- **实现（先改权威副本 `deploy/deploy/`，再整体同步三套）**：
  - JS(npc-stories.js)：新增规则对话引擎 `npcReply(pid,q,text)`——按**故人性格(p.a: BADMINTON/CAREER/BODY/MIND) × 关键词主题(music/sport/career/body/struggle/good/calm)**选回应，开场白随关系等级(初识/相识→熟识+ 用更亲近的 opener)，60% 概率追加一个追问保持对话循环；`NPC_CHAT_TOPICS`/`NPC_CHAT_OPEN`/`NPC_CHAT_BYATTR`/`NPC_CHAT_GENERIC`/`NPC_CHAT_CLOSE` 数据驱动。空输入有兜底（"我在听。你愿意多说两句吗？"）。
  - JS(systems.js)：`renderNpc()` 每张委托卡加「💬 聊聊」按钮（显示已聊回数）+ 可折叠对话区（线程 `q.chat` + textarea 发送）；新增 `npcChatOpen`(Set, 会话内保持展开) / `npcToggleChat(qid)` / `npcSend(qid)`（读取输入→push 我的消息→`npcReply`→push 故人回应→`npcRel(know)+1`、前 10 次对话各 +2 bonusXP→`save();render()`）。用户输入经 `escHtml()`（tasks-render.js:1540 已有全局）转义防 XSS/布局破坏。
  - 数据模型：`npcRel(pid)` 默认加 `know:0`（了解度，跨周累积在 NPC 上）；`npcRelInfo` 返回透出 `know`；`npcMemory` 在 know>0 时追加"你们断断续续聊过 N 回，他记下了你的一些小事。"；委托卡关系行显示「· 聊N」。对话线程 `q.chat` 存在当周委托上（周刷新后重置，但 know 永久累积——符合"故人记得你"的叙事）。
  - CSS(life-rpg.css)：`.npc-chat`/`.npc-chat-msgs`/`.npc-msg.me|.npc`/`.npc-msg-t`(金底=我/灰底=故人, pre-wrap 保留换行)/`.npc-chat-compose textarea` 样式。
  - index.html 版本戳 `?v=5.47.0`→`?v=5.48.0`（5 处：css link + 4 个 script）。
- **验证**：① 三套 JS `node --check` 全过；② vm 冒烟测试 `npcReply` 对 bailu/lin/shen/yun 各类输入(含空输入)均正常返回、know 透出正常；③ 三套 JS 的 npcReply/npcSend/npcToggleChat/open-chat 均就位；④ 三套 HTML 版本号均 5.48.0、无 5.47.0；⑤ 三套 CSS `.npc-chat-compose` 均存在。
- **设计取舍**：本 RPG 是纯静态本地文件，**无 LLM 后端**，故故人回应走"规则+关键词"引擎（无法真正理解语义）。若日后要 AI 真·理解式对话，需给文件接一个 API（带 key + CORS），不在本次范围。规则引擎已覆盖琴/曲/球/职/累/卡/顺 等主题，且随关系加深回应更个人化。

## 12:3x 年目标自由记录+重分析 / 羽毛球 0 小时 bug 修复 / 拆打球·基本功（v5.49.0）
- **用户需求（三件）**：① 年度主线（longterm 年目标）应能「随时记录进展 + 显式重新分析当前进度」；② 羽毛球「近 8 周周均打球 0 小时」是数据 bug，要修；③ 羽毛球数据要拆「打球 / 基本功」两个维度，不要混在一起。
- **AskUserQuestion 拍板**：Q1 年目标记录=「自由记录+重分析（推荐）」→ 每张年目标卡加自由文本记录区 + 显式「重新分析」按钮；Q2 羽毛球拆分=「能拆的拆、拆不动的按比例（26 年前约一周基本功 2h、其余打球）」。
- **实现（先改权威副本 `deploy/deploy/`，再整体同步三套）**：
  - `core-state.js`：加拆分常量 `BADMINTON_LIFETIME_HOURS=1649`；`BM_PLAY_BASE=Math.round(1649*2/3*60)`、`BM_BASIC_BASE=1649*60-BM_PLAY_BASE`（即打球 1099h / 基本功 550h，按 2/3·1/3 拆，无损 1649）。
  - `systems.js`：① `LIFE_TRACKS` 把 `badminton` 改名「羽毛球·打球」(base:BM_PLAY_BASE)，新增 `bmbasic`「羽毛球·基本功」(base:BM_BASIC_BASE, rec:20)；② `lifeTrackOfTask` 映射：基本功/步法/专项/多球训练/技术练习/挥拍练习→`bmbasic`，羽毛球/打球/对抗/比赛/实战→`badminton`；③ 新增迁移 `migrateBmSplit()`（置 `migr.bmSplit` 标记，把旧 1649h 基数拆入 `lifeCompound.bases.badminton=BM_PLAY_BASE`、`.bmbasic=BM_BASIC_BASE`，自适应 `ensureLifeCompound`+`save`），init 调用；④ 建议逻辑「今天练过球了」改为 `practiceTodayMinutes('badminton')>0||practiceTodayMinutes('bmbasic')>0`。
  - `tasks-render.js`（`_rpg_bmfix.py`，4 处 replace_once 断言）：① `yearGoalAnalysis` 中 `badminton` 的 `track.base` 用 `(BM_PLAY_BASE+BM_BASIC_BASE)`（终身合并）；② **0 小时 bug 根因修复**——原 `windowAttrMinutes(S.weekly,...)` 只扫周任务清单，羽毛球真源是 `S.lifeCompound.logs` → 改用新函数 `badmintonAvgHours(key,weeks)`（读复利轨道日志近 N 周分钟/60/weeks）；③ 新增 `badmintonAvgHours` / `yearRecordBlock(c,i)`（文本框+记录/重分析按钮+最近记录）/ `saveYearRecord(i)`（push `c.records`）/ `reanalyzeYear(i)`（renderLongterm）；④ `yearAnalysisCard` 的 advice 块后插 `yearRecordBlock(c,i)`。
  - `life-rpg.css`：加 `.ya-rec`/`.ya-rec-h`/`.ya-rec-ta`/`.ya-rec-actions`/`.ya-rec-last`/`.ya-rec-ts`/`.ya-rec-empty`/`.ya-rec-cnt` 样式。
  - `index.html` 版本戳 `?v=5.48.0`→`?v=5.49.0`（5 处：css + 4 js）。
- **验证（关键结论）**：
  - vm 冒烟测试：打球周均=1.50h、基本功周均=0.50h、合计=2.00h；历史拆分 1649h（打球 66.7% + 基本功 33.3%）；**空日志周均=0（非 NaN，0 小时 bug 已修）**。MIGRATION PASS：迁移后 bases.badminton=1099h、bases.bmbasic=550h。
  - 三套一致 grep：bmbasic / BM_PLAY_BASE / migrateBmSplit / yearRecordBlock / saveYearRecord / reanalyzeYear / badmintonAvgHours 三套均就位；`windowAttrMinutes(S.weekly` 残留=0；版本号均 5.49.0。
  - `node --check`：三套 core-state/systems/tasks-render/npc-stories 全过。
- **脚本留存**：`_rpg_bmfix.py`（replace_once 断言，非幂等——重跑会失败，勿重复执行）。
