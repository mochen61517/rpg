# 2026-08-03

## life-rpg v5.13.0：新增「人生地图」页
- 用户方向确认：地图先做；装备=数值生效型（不做装饰，装饰太像徽章）；之后加「随机掉落的自助奖励」（按金钱/时间成本分级：小成就小奖励、大成就大奖励，如大餐/蛋糕/球鞋）。
- 实现：新增 🗺️ 人生地图 导航 + `#page-map` 页。复用四大领域 ATTRS（羽道🏸/业道💼/体道💪/灵台🎹）。
  - `renderMap()`：顶部「世界之心·总探索度」(四大疆域平均等级 + 总进度%)，下方 4 块疆域卡(等级/进度条/状态chip 今日精进·待修·待启/本周时长/今日项数)，点击疆域跳角色设定页。
  - 数据全复用四系状态逻辑：`lvlOf(S.attrs[k])`/`xpInLvl`/`weeklyAttrMinutes`/`todayAttrStatus`/`h`。
  - 仪表盘「四系当前状态」面板加 👉 人生地图 联动入口（点击跳 map）。
- 验证：node --check 通过；node DOM 桩实测 renderMap 生成 hub+4 疆域+pct，空数据无报错。
- 版本号 v5.13.0（title + 品牌文本同步）。deploy/index.html 本地提交 a618003。
- 待办（用户已定方向，未实施）：①装备数值生效型设计；②随机掉落自助奖励系统（金钱/时间成本分级）。

## life-rpg v5.14.0：旅行地图（替换原"人生地图"）
- 用户要求把地图换成"世界地图"：通过旅行解锁地点、写感受；年/月任务可定去某地旅游。
- 合规约束（geo-map-compliance-guard）：禁用 Google/OSM/Leaflet/Mapbox 等境外地图服务；合规源(腾讯/高德/天地图/百度)需 key，而本页是部署到 GitHub Pages 的单文件离线应用，运行时代理 `_TMapSecurityConfig` 仅 WorkBuddy 预览内有效、部署后拿不到 key。故采用**内嵌自绘简化世界地图 SVG**（等距投影 viewBox 1000×500），不依赖任何地图服务；并严格合规：台湾作为中国一部分(land 同色+单独"台湾"标注)、含南海诸岛(九段线示意+岛点+"南海诸岛"标注)。
- 实现：
  - `TRAVEL_PLACES` 常量：17 个地点(国内9+亚洲/欧洲/欧亚/极地)，含经纬度(投影定位)、region、wish(是否人生愿望目的地)。
  - `renderMap()`：画 svg 世界地图(陆地/经纬网/中国+台湾/南海诸岛/引脚)；点击圆点打开编辑弹窗。
  - 地点解锁：`S.travel[id]={visited,date,rating,refl}`；弹窗可标记到访/撤销、填日期、1-5星评分、写感受(escapeHtml 转义防 XSS)。
  - 「我的足迹」卡片列表(星级+感受+日期)；顶部统计 足迹数/解锁率。
  - 年/月旅行目标：`S.travelGoals={year,month}` 下拉选地点，达成(已到访)自动显示 ✅。
  - 导航"🗺️ 人生地图"→"🗺️ 旅行地图"；仪表盘四系面板链接文字同步。
- 验证：node --check 通过；DOM 桩实测 renderMap 含 svg/引脚/中国/南海诸岛、足迹转义正确、年目标达成标识正确、到访/撤销/保存流程正确(评分/感受/日期落库)。
- 版本号 v5.14.0；deploy/index.html 本地提交 64311e8。
- 发布同前：沙箱弹不出 GitHub 凭证，push 失败，需用户本机 `git push origin main` 或给 PAT。发布后 Ctrl+F5 清 CDN 缓存。
- 待办仍有效：①装备数值生效型；②随机掉落自助奖励。

## life-rpg v5.14.1：旅行地图分区切换
- 用户反馈：单张世界地图太挤、看不清，要求顶部可切换分区（中国/东南亚/欧洲/美洲/澳洲/北极洲/南极洲）。
- 方案：保留世界等距投影与自绘 WORLD_LAND 轮廓（避免重绘/境外服务），切 tab 时只改变 SVG `viewBox` 放大该区域子矩形（比例锁定 2:1 避免变形），引脚只画本区地点（其它区隐藏），中国 tab 才显示"中国/台湾/南海诸岛"标注。复用现有 projX/projY 坐标天然对齐轮廓。
- 数据：每个 `TRAVEL_PLACES` 加 `zone` 字段；新增 `MAP_TABS`(7 区：cn/sea/eu/am/au/n/s，各含 bounds 或 pole)；`S.mapTab` 持久化当前分区（默认 cn）；`renderMapTabs()`/`setMapTab()` 渲染并切换 tab（带各区地点数角标）。日本归 sea 区（标注"含东亚"）、土耳其归 eu；美洲/澳洲目前无地点→显示"本区暂无规划地点"空态。
- 验证：Node DOM 桩对 7 区逐一渲染——引脚数=各 zone 期望数(9/1/5/0/0/1/1)、空区有提示、viewBox 比例均≈2:1、中国视图覆盖中国坐标、两极各 1 点不崩。全部 ALL OK。
- 版本号 v5.14.1（title + 品牌"v5.14.1 · 旅行地图分区"）；deploy/index.html 本地提交 3ea8a88。
- 发布：沙箱无 GitHub 凭证，push 仍需用户本机 `git push origin main` 或给 PAT；发布后 Ctrl+F5 清 CDN 缓存。
- 待办仍有效：①装备数值生效型；②随机掉落自助奖励。

## life-rpg v5.14.2：中国tab改绘带省级边界的中国地图
- 用户反馈：原"中国"tab 只是世界粗糙轮廓放大的一块，省市都没呈现，看着空、不像中国地图。
- 方案：给"中国"tab 单独画一张带省级边界的中国地图（自绘 SVG，静态数据、不连任何境外地图服务，台湾作为中国一部分+含南海诸岛）。数据源=阿里 DataV 国产 GeoJSON（`https://geo.datav.aliyun.com/areas_v3/bound/100000_full.json`，沙箱可拉取），Node 脚本转成投影到本地坐标系(等距投影 lng73-135/lat10-53.5，CH_W=1000/CH_H=702)的省级 path，舍去微小岛屿→`CHINA_PROV`（32 个省级单元，含台湾省、已排除南海诸岛单独手绘）。
- 实现：`renderMap()` 增加 `tab.id==='cn'` 分支——画省界 path(.cn-prov)+省名标注(去后缀)+九段线(.scs)+南海诸岛标注+本区 15 个目的地引脚(用 chX/chY 投影，与世界 tab 的 projX/projY 分离)；其余分区保持 v5.14.1 的世界轮廓+viewBox 放大逻辑不变。新增 6 个中国目的地(西安/敦煌/张家界/厦门/哈尔滨/西宁)让省市更完整。
- 验证：整段脚本 new Function 语法 OK；Node DOM 桩实跑 renderMap('cn')——32 省界路径、15 引脚、九段线/南海诸岛/台湾/新疆/北京均在、30 个坐标全落 viewBox 内(0 越界)。ALL TESTS PASSED。
- 版本号 v5.14.2；deploy/index.html 本地提交 db00cda。发布仍同前：需用户本机 push 或给 PAT，Ctrl+F5 清缓存。
- 待办仍有效：①装备数值生效型；②随机掉落自助奖励。

## life-rpg v5.14.3：地图支持自定义地点（直接输入文字 + 任意处打点）
- 用户反馈两点：①"已到访让我也可以直接输入文字吧"——希望自行新增不在预设列表里的地点；②"这个原点有些地方没有"——预设 pin 只覆盖有限目的地，很多想去/去过的地方没圆点。
- 方案：新增"＋新增地点"按钮（mapTabs 末尾）。点它进入新增模式（地图 cursor=crosshair、显示提示）→ 点地图任意空白处在 SVG 用户坐标处反投影出经纬度（`onMapClick`：cn 用 chX/chY 逆运算、世界/极地 tab 用等距投影逆运算）→ 弹 `customModal` 让用户填【地点名称*】【副标题】【所属分区】【已去过 checkbox+日期+1-5星+感受】→ `saveCustomPlace` 存 `S.customPlaces`（localStorage 持久化）。
- 关键设计：
  - `S.customPlaces=[]`（state 初始化 + ensureState 兜底）；自定义项结构 `{id:'c_'+Date.now(), name, sub, region:'自定义', zone, lng, lat, wish:false}`。
  - `getPlace(id)` 先查 TRAVEL_PLACES 再查 customPlaces，使原有 `openPlaceEditor`/`savePlace`/`toggleVisitedFromModal` 对自定义地点通用（travel key=id 不变）。
  - `renderMap` 用 `placesInZone(zone)`（TRAVEL_PLACES+customPlaces 按 zone 合并）画 pin；统计(足迹数/解锁率)与足迹卡片、年/月目标下拉均合并 customPlaces。
  - 自定义地点编辑入口：`openPlaceEditor` 打开时显示"删除"按钮（placeModal 的 #pmDelete，调用统一 `deleteCustomPlace`，清 customPlaces + S.travel）。customModal 自身也有删除。
  - 防 XSS：pin 标签/足迹卡片/目标下拉对 name 用 escapeHtml；customModal 用 .value 而非 innerHTML。
  - onMapClick 先判 `evt.target.closest('.pin')` 防误触已有 pin；非新增模式点空白无操作。
- 验证：Node DOM 桩全流程——23 预设(15cn+8世界)；点击中国区得 104°E/41.7°N 并写入、坐标展示正确；保存后 cpLen=1、travel.visited=true、感受转义(footprint 含 &lt;script&gt; 无裸标签)；删除清理 travel；sea/eu/am/au/n/s 各 tab 渲染不崩；欧洲新增 zone=eu/lat=43.2。ALL TESTS PASSED。
- 版本号 v5.14.3（title + 品牌"v5.14.3 · 旅行地图·自定义地点"）；deploy/index.html 本地提交 9048767。
- 发布仍同前：需用户本机 `git push origin main` 或给 PAT，Ctrl+F5 清 CDN 缓存。
- 待办仍有效：①装备数值生效型；②随机掉落自助奖励。

## life-rpg v5.15.0：战利品页（装备数值生效型 + 随机掉落自助嘉奖）
- 用户指令"1和2挨个做"：先做完数值生效型装备系统，再做随机掉落自助奖励。合并为新增 🛡️ 战利品 页（两个 tab：装备库 / 嘉奖箱）。
- **装备系统（数值生效，非装饰）**：
  - 数据：`S.equips={owned:[6个默认全owned],equipped:[]}`；`S.customEquips=[]`；`EQUIPS` 常量 6 件（竞速球拍/精进手册/蛋白补给/黑胶唱片/专注指环/旅行斗篷），每件 `{id,name,icon,attr,bonus,rarity,desc}`，attr∈{BADMINTON,CAREER,BODY,MIND,ALL}，bonus=经验加成倍率（0.08~0.12）。
  - 数值生效点：`grant(attr,xp,neg)` 改为 `eff=xp*(1+equipBonusFor(attr))`，即穿上后该领域「获得经验 ×(1+加成)」——控制新增经验速度（不改已存 XP），符合"数值生效"语义。
  - `equipBonusFor(attr)` 累加已装备中 attr 匹配或 ALL 的 bonus；每领域同时仅 1 件（装备同 attr 冲突提示先卸下），ALL 计入通用槽。
  - 交互：`equipItem`/`unequipItem`/`addEquip`(自制装备，存 customEquips)/`delEquip`；页面展示加成汇总 + 每件装备卡（装备/卸下/删除）。
- **随机掉落自助嘉奖**：
  - 数据：`S.rewards={drops:[],dailyCount:0,dailyDate:''}`；`REWARDS` 常量 10 项，按成本分级 tier∈{micro(≤¥60/<1h),small(≤¥200/≈2h),medium(≈¥600-800),big(≥¥1200/数h~数天)}，例：蛋糕/奶茶/书/大餐/电影/SPA/数码/球鞋/演唱会/短途旅行。
  - `dropReward(tier,reason)`：从对应 tier 池随机取一条写入 drops，附 ts/reason；micro/small 受每日上限 3 次（避免刷屏），big/medium 不限。
  - 触发点：① 等级突破 `render()`（span≥3→big / ≥2→medium / 否则 small）；② 点亮人生愿望 `toggleWish`（medium）；③ 完成日常任务 `toggle` 12% 概率（small 30%/micro 70%）；④ 完成年度大项 `toggleChecklistItem` 20% 概率（big 35%/medium 65%）。
  - 交互：`claimReward(idx)` 标记"已享用"；嘉奖箱按待领取/已享用排序展示，含成本估算(¥/h)与掉落原因。
- 验证：Node DOM 桩跑通核心链路——initBonus=0、grant 无加成 100+10=110、装备球拍后 bonus=0.1、再 grant 110+11=121、BODY 装备后 0.08、奖励每日上限(第4次 micro 返回 null)、big 不受限、claim 标记、renderLoot/Equips/Rewards 渲染无异常。修复：模板字面量内嵌套反引号导致语法错→改字符串拼接。
- 版本号 v5.15.0（title + 品牌"v5.15.0 · 战利品·装备&嘉奖"）；deploy/index.html 本地提交 618f7d2。
- 发布同前：沙箱无 GitHub 凭证，需用户本机 `git push origin main` 或给 PAT；发布后 Ctrl+F5 清 CDN 缓存。
- 历史待办（装备/奖励）均已完成。下一步可关注：装备/奖励数据是否要并入自动备份；或继续其它人生 RPG 模块。

## life-rpg v5.15.1：新增地点改为「输入地名→自动定位」（修用户纠正）
- 用户纠正 v5.14.3 的"盲点地图任意处打点"：应该输入地名，由系统自动在地图对应位置点亮，否则用户根本不知道点对应现实哪。
- 方案：地名库 `CITY_COORDS`（原名 CITY_LIB，302→321 条，中国主要城市/景点+世界主要城市+极地，含拼音/英文别名与经纬度/zone）。新增搜索弹窗 `addSearchModal`：`openAddSearch()`→输入实时 `findCoord(q)` 匹配候选→`pickCity(i)` 用候选真实经纬度+zone 自动创建 `S.customPlaces` 并切到对应 tab（如大理→cn/东京→sea），再弹 `customModal` 填到访/感受。无匹配则显示"在地图上手动点选"兜底（`manualAddMode`，保留原 onMapClick 反投影）。tab 按钮"＋新增地点"改为 `openAddSearch()`；提示文案同步更新。
- 边界一致性大修：`MAP_TABS` 边界重设（sea 扩为覆盖整个东半球 [[-20,160],[-40,60]] 作兜底、eu/am/au 放宽），并新增 `geoZoneFor(lng,lat)` 与边界一致；重算全部 321 城 zone 使其严格落在所属 tab 边界内（校验 OUT=0）。这样搜任何城市点亮后必在对应 tab 可视范围。
- 修复：`_rezone_cities.js` 注入 geoZoneFor 的正则误吞相邻 `defaultGoals()` 函数（被删），已恢复 `function defaultGoals(){return {year:null,month:null};}`（语法/初始化报错因此而来）。
- 版本号 v5.15.1（title + 品牌"v5.15.1 · 旅行地图·地名自动定位"）；deploy/index.html 本地提交 925d018。
- 发布同前：沙箱无 GitHub 凭证，需用户本机 `git push origin main` 或给 PAT；发布后 Ctrl+F5 清 CDN 缓存。根目录残留 _augment_cities.js/_rezone_cities.js/_test_search.js/_check.js 测试脚本（沙箱安全删除拦截未清，不影响功能，可手动删）。

## life-rpg v5.15.2：疗愈类任务 21 天间隔约束（头疗/按摩/康复理疗）
- 用户习惯规则：头疗、按摩、康复理疗这类"疗愈"类活动，一周不会超过一次，且每次间隔至少 21 天（不连续两周做头疗/不连续两周做按摩）。
- 关键设计：间隔约束**不能依赖 `donedates`**（每周重置 2351 行会清空它），故新增独立长期历史字段 `item.lfDates=[]`，重置逻辑不碰它，跨周跨月仍有效。
- 实现：
  - 辅助函数 `lfGapDays(item)`（返回 21 或 0：优先 item.minGapDays 字段；否则正则 `/头疗|按摩|康复|理疗/` 匹配任务文本→21，其它→0）、`lastLfDate(item)`、`lfDaysLeft(item,d)`（距上次天数<gap 时返回还需天数，否则 0）。
  - `toggle()` 改造：`nowDone=!was` 提前；勾选完成前若 `lfDaysLeft(item,d)>0` 则 `alert` 拦截并 `return`（不动 donedates、不记 XP）；成功后把 d 写入 `item.lfDates`。
  - 任务卡片 `renderTaskList` 两分支(qmeta)对处于冷却期(`!done && lfDaysLeft>0`)的低频任务显示 `⏳ 冷却中·还需N天`(`.qcool` 橙底样式)。
- 注意：defaultTasks 里那三条疗愈任务（去做一次 80 分钟全身按摩/康复理疗/头疗）是 weekly 类型，走 `toggle(S.weekly)`，关键词匹配即可生效；用户自定义命名含"按摩"等词的也会自动受约束。眼部护理未点名，未纳入（如需可加正则）。
- 验证：Node DOM 桩 ALL PASS——按摩 gap=21、普通任务 gap=0、距14天→left=7、距24天→left=0、间隔不足弹窗拦截且 donedates 不写入、满间隔写入 lfDates+donedates（render 抛错被 try/catch 兜住，不影响 lfDates 写入，系测试桩 S.goals 所致、与改动无关）。
- 版本号 v5.15.2（title + 品牌"v5.15.2 · 疗愈间隔约束"）；deploy/index.html 本地提交 2af0130。
- 发布同前：沙箱无 GitHub 凭证，需用户本机 `git push origin main` 或给 PAT；发布后 Ctrl+F5 清 CDN 缓存。根目录残留 _test_lf.js 测试脚本（沙箱安全删除拦截未清，可手动删）。
