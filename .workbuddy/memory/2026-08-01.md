# 2026-08-01

## life-rpg 勾选完成庆祝弹窗（v5.12.3 → v5.12.4）
- 用户要求：每完成一项打卡/补剂，弹出与该项内容相关的庆祝文案（例：电解质→"今天运动表现稳了"；姜黄饮→"今天抗炎+1"）。
- 实现：
  - 新增 `SUPP_MSG`（11 条补剂精确文案：维生素D/镁/鱼油/维C/氨糖/姜黄饮/姜黄奶/羽衣甘蓝粉/奇亚籽/甜菜根粉/电解质粉）、`CAT_MSG`（7 条轶事分类：社交/创作/美丽/疗愈/灵宠/养生/灵感）、`ATTR_MSG`（6 条属性兜底：BODY/STUDY/MIND/MONEY/HEART/SPIRIT）。
  - `findCelebrate(t,key)`：精确 t 匹配 → 去掉 粉/饮/奶 等后缀再匹配 → CAT_MSG[key] → ATTR_MSG[key] → 默认"✔ 又完成一项"。
  - `celebrateTask(msg)`：在 `#fxLayer` 内显示居中 toast（`.task-celebrate`，顶部 15%、渐变紫蓝、淡入上浮、2.3s 自动消失），不挡操作（fxLayer pointer-events:none）。
  - 在三个勾选完成分支挂载：`toggle()`（daily/weekly/monthly/补剂）、`toggleSide()`（轶事）、`toggleChecklistItem()`（周月年清单）。
- 验证：node --check 通过；node 单测 9 个用例（含用户两个例子 + 兜底）全部命中。deploy/index.html 本地提交 6a5becf。
- 发布：push 仍受沙箱无 GitHub 凭证限制失败，待用户在自有环境 `cd deploy && git push origin main` 或网页上传。
