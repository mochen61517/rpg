# 2026-07-30 工作日志

## 数据丢失排查（用户反馈"昨天记录的数据都没了"）
- 现象：用户 7-30 打开 life-rpg.html 发现 7-29 记录的任务勾选/经验/小时都没了。
- 排查：项目目录、C:\Users\ZhuanZ1\Downloads、Desktop 均无导出的 lifeRPG_*.json 备份 → 数据未落盘，仅存浏览器 localStorage。
- 根因判断（非代码 bug）：localStorage 按 origin（文件位置/网址）+ 浏览器隔离。换文件、换浏览器、用 WorkBuddy 预览面板、清缓存/无痕 都会让"当前位置"读不到昨天的存档。我的迁移代码（含 monthOutcomeClean_v1）只清理月主线结果型项，不清日常/经验，不是丢失原因。
- 恢复路径（已告知用户）：回到昨天用的「同一浏览器 + 同一打开方式」（双击同一个 HTML / 同一网址）→ 数据应还在 → 立即「导出存档」下载 JSON → 在当前文件「导入存档」恢复。若回到原环境也看不到，说明该位置存储已被清，只能重录。
- 待办/建议：用户强烈需要防丢机制。后续可加「自动备份开关」（每次保存自动下载 save.json 到指定目录，或每天首次打开提示导出）。需在用户授权写文件路径后再实现。

## 用户确认：两个 file:// 路径均空 → 数据确已清空（非隔离切换）
- 用户用 Google Chrome 双击本地 HTML 记录（7-29 23:11 还在反馈，当时数据在）。
- 7-30 分别在 `life-rpg.html` 与 `deploy/index.html` 两个 file:// 路径打开，均显示空 → 排除"换文件看错"，确认该 Chrome 的 file:// localStorage 被清空。
- 文件层面（项目目录/Downloads/Desktop）无任何导出备份 → 无法从文件找回，本次数据真实丢失。
- 根因推测：Chrome 清了站点数据 / 关浏览器清 cookie 和站点数据 / 切换了 Chrome profile / 用过无痕。具体未知。
- 结论：file:// + localStorage 作为持久化方案不可靠（MDN 注明行为未标准化、按绝对路径隔离、可能被清）。必须改为本地文件落盘才能真正防丢。
- 待决策（已向用户提出两方案）：① 本机 localhost 服务器 + File System Access API 自动读写本地 save.json（彻底，需起服务器+授权一次）；② 保持 file:// + 加强导出提示（零部署，手动备份）。等用户选择。

## 已实现方案①：本机 localhost + File System Access 自动落盘
- 用户选「本机 localhost 自动落盘（推荐）」。目标：彻底摆脱 file://+localStorage 易丢问题。
- 代码改动（life-rpg.html）：
  - 新增 FS 持久化层：`FS_AVAILABLE=('showSaveFilePicker' in window)`；`_idb`（IndexedDB 存 file handle，刷新免重选）；`chooseSaveFile()`（showSaveFilePicker 授权 lifeRPG_save.json）；`fsWrite()`/`fsRead()`（createWritable 读写）。
  - `load()` 改 async：优先从授权文件读，否则 localStorage 兜底；末尾仍写 localStorage 兜底备份。
  - `save()` 改 async：写 localStorage + 若已授权则写文件。
  - init 改 async IIFE：`await load()` 后渲染，FS 可用且未授权时显示「📁 选择存档文件」提示横幅。
  - UI：按钮区加「📁 选择存档文件」；footer 文案更新为「优先本地文件，未授权降级 localStorage」；新增 `fsBanner` 横幅。
- 新增 `start_server.bat`：用本机 managed python（`C:\Users\ZhuanZ1\.workbuddy\binaries\python\versions\3.13.12\python.exe`）起 `http.server 8080`，目录为 deploy（server 根即 index.html）。localhost 是 secure context，FS API 可用；本机访问稳定（不同于之前手机局域网 IP 死法）。
- 同步 deploy/index.html（server 入口）。
- 验证（Node DOM 桩，ALL PASSED）：① 旧存档 load 兜底正确 + month 迁移 + daily 迁移补项；② 全新默认；③ save 返回 Promise 不崩；④ 无 FS 环境 chooseSaveFile 降级不崩。FS 实际落盘行为需浏览器通过 localhost 打开并授权后实测（Node 无法测）。
- 注意：手机访问仍走 localStorage 兜底（局域网 IP 非 secure context，FS 不可用），手机记录的数据在电脑已授权 FS 后以本地文件为准——手机同步需另做（当前非重点，用户主用电脑记录）。
- 待办：用户通过 localhost 实测落盘；之前丢失的 7-29 数据无法找回，需重录。

## 修复：补录/切回今天 已勾选项被覆盖（v5.3）
- 现象：用户补录过去某天后点「今天」切回，原来今天已勾的选项没了（或反过来，补录污染了今天的勾选）。
- 根因：旧模型每个任务只有一个 `done` 布尔 + `doneDate`，补录/今天共用同一开关，互相覆盖。
- 改动（life-rpg.html 已实装 + 同步 deploy/index.html）：把完成状态改为**按日期存储** `donedates:[日期]` + `mins:{日期:分钟}`。
  - 新增辅助：`isDone(x,d)` `setDone(x,d,val)` `isDoneEver(x)` `lastDoneDate(x)` `itemXpAt(it,d)` `weightedXpAt(it,d)`。
  - `toggle`/`toggleChecklistItem`/`listHtml`/`checklistBlock`/`render`(日主线+mainQ)/`pushWechat`/`pickMain`/tier 概览/周月重置/newDay 全部改用日期感知；时间型任务分钟也按日期存（`mins[d]`），避免补录日把今天时长覆盖。
  - 迁移 `migrate()` 新增 convDates：旧 `done(bool)+doneDate` → `donedates=[doneDate||今天]`、`mins`（时间型填回），并 `delete done/doneDate`。年主线简单目标 `c.done`（终局达成标记）保留不动。
  - newDay：日常/补剂不再清空（按日期天然每天空），仅每周一/每月初清空周/月主线 donedates（重置逻辑）。
- 验证（Node DOM 桩，ALL PASSED）：① 全新档迁移出 donedates 且无 legacy done；② 同日任务在 d1、d2 各勾一次，切回 d1 仍勾、d2 保留（核心 bug 修复）；③ 固定型补剂两日期独立；④ 连击今日+昨日=2、仅今日=1；⑤ 旧 `done:true` 存档迁移到 donedates、今天视图不误显。
- 版本号：title/sub 标 v5.3。

## Gitee 部署 + 云端同步（v5.4）
- 用户注册 Gitee 并给仓库地址 https://gitee.com/desire-deeply/mochen-rpg.git（owner=desire-deeply, repo=mochen-rpg）。诉求"部署更稳"。
- 澄清：Gitee Pages 只解决访问稳定（公网 https 固定地址，不用跑 localhost，手机电脑同 URL）；打卡数据仍存浏览器，真防丢靠已有导出存档 + 本地文件落盘 + 新加的 Gitee 云备份。
- 已加「☁ Gitee 云备份」折叠面板（静态 HTML，不被 render 重建）：owner/repo/token/path 输入框 + 保存配置/备份/恢复三按钮。owner/repo 已按用户仓库预填默认值。
- 实现：backupToGitee（GET 拿 sha→PUT 写 data/save.json，base64 UTF-8）、restoreFromGitee（GET 解码覆盖本地，confirm 防误覆盖）、ensureGiteeCfg（输入框读并存 S.gitee）、fillGiteeInputs（init 回填）、utf8ToBase64/FromBase64（TextEncoder+btoa 处理中文/emoji）。
- 验证（Node DOM 桩 mock fetch，ALL PASSED）：base64 中文+emoji 往返、ensure 读输入框、backup 创建(404→无 sha)/更新(带 sha)、token 经 query、restore 替换 S。
- 风险/CORS：Gitee API 前端 fetch 可能跨域受限，已 panel hint 标注；若 CORS 失败改用「导出存档」+ 手动传仓库兜底。需用户浏览器实测。
- 免费 Gitee Pages 仅支持**公开**仓库；若 mochen-rpg 为私有需改公开或付费 Pages Pro（云备份 API 不受私有限制）。我无法代 push（无用户 Gitee 凭证）。
- 部署步骤：公开仓库根目录放 index.html（deploy/index.html 已同步）→ 服务→Gitee Pages 选默认分支+根目录+强制 HTTPS → 实名→启动→得 *.gitee.io 地址 → 改 HTML 后 push 需回 Pages 点「更新」。
- 同步 deploy/index.html。footer 文案改通用版。待办：用户实操部署 + 生成私人令牌(projects) + 实测 CORS。

## 改用 GitHub Pages 部署成功（弃 Gitee Pages 入口难找）
- 用户反馈 Gitee「服务」菜单无 Gitee Pages 入口（可能需实名/公开仓库才显，用户未深究）。改在 GitHub 新建公开仓库 https://github.com/mochen61517/rpg 上传 deploy/index.html，Settings→Pages→Deploy from a branch→main+/root→Save，得可访问地址 https://mochen61517.github.io/rpg/ （自动重部署，比 Gitee 省一步）。
- 用户确认 github.io 地址可访问，但"数据又没了"：根因 **localStorage 按 origin 隔离**——localhost:8080 与 github.io 是两个独立存储房间，之前在 localhost 记录的打卡不会出现在 github.io；换浏览器/手机同理。非真丢。
- 已指导迁移：同电脑开 localhost:8080 → 导出存档 save.json → github.io 版导入存档。并警告：以后固定只用 github.io 一个地址，别 localhost/github.io 混用（两边不互通）；根治靠开云备份（当前页面面板预填的是 Gitee，已提议改 GitHub 版云备份同账号少记一个平台，待用户决定）。
- 结论：网页托管层改用 GitHub Pages 稳定；数据稳定性仍靠 ① 导出/导入手动搬 ②（建议）云端备份。CORS 风险从 Gitee 转 GitHub API 待实测。

## 云备份从 Gitee 改为 GitHub 版（v5.4.1）
- 用户确认"固定用 GitHub 版本"= 不要 Gitee 存数据，统一 GitHub，少记一个平台。
- 改造 life-rpg.html：HTML 面板 id 前缀 g→gh、文案 Gitee→GitHub、owner/repo 默认预填 mochen61517/rpg；JS 函数 ensureGiteeCfg/saveGiteeCfg/backupToGitee/restoreFromGitee/fillGiteeInputs 全改为 gh 版。
  - 关键差异：GitHub Contents API `https://api.github.com/repos/{o}/{r}/contents/{path}`；认证用 `Authorization: Bearer {token}` 头（非 Gitee 的 URL query），更安全且 api.github.com 原生支持 CORS，前端 fetch 一般无跨域问题；GET 拿 sha / PUT 写（带 sha 更新，404 则创建）。
  - token 权限：classic PAT 勾 `repo`，或 fine-grained 勾该仓库 Contents 读写。
- 验证（Node DOM 桩 mock fetch，ALL PASSED）：base64 中文+emoji 往返、ensure 读输入框、backup 创建(404→无 sha)/更新(带 sha)、token 经 Bearer 头（不在 URL）、restore 替换 S、fillGhInputs 回填。
- 同步 deploy/index.html。待办：用户重新上传 deploy/index.html 到 github.com/mochen61517/rpg（覆盖）；生成 GitHub PAT 填面板实测备份/恢复。

## 人设叙事 / 游戏感改造（v5.5 北城灯客）
- 用户要求按自己人物画像加人设、讲故事、更有游戏感。基于画像设计"北城灯客·丁火命格"叙事框架（威动六载→rankai 漂泊→寻锚点门派安身 / 云水之乡云南归处 双线大道）。
- 落地 life-rpg.html：
  - 新增角色卡 panel（.hero CSS + HTML）：角色名祁沫辰/Mochen、称号"北城灯客·丁火命格"、修为境界(#realmLvl)、动态章节(#chapterText)、可折叠「角色背景·北城灯客修行录」故事。
  - 新增 chapterText()：按年主线[0]（安身）完成状态返回"第一卷·寻锚 / 第二卷·立基"动态章节，叠加云南探索天数。
  - ATTRS 改名四系：羽毛球→羽道、职业→业道、身体健康→体道、精神享受→灵台（含 desc 游戏化）；goals 名保持原主题未动。
  - HTML 标题游戏化：每日主线→每日功课、日课/周常、本周修行/本月修行/今年大道；连击"连续X天"→"灯火不熄X日"。
  - ACH 成就名游戏化：七日节律→灯火·七日不熄、职业推进→术法精进、羽毛球千小时→羽化千里、上岸→立基·上岸、云南初体验→云水初临 等。
  - celebrate() "LEVEL UP!"→"境界突破！"；title/sub/foot 统一 v5.5。
- 验证（Node DOM 桩，ALL PASSED）：render 不崩、ATTRS=羽道/业道/体道/灵台、chapterText 卷一/卷二切换、realmLvl/chapterText 填充。
- 同步 deploy/index.html。待办：用户重新上传 deploy/index.html 覆盖 github 仓库（含 v5.4.1 GitHub 云备份 + v5.5 人设）；之后 github.io 即新版本。

## 多页重构 + 人设脱敏（v5.6）
- 用户反馈：单页太挤；要左侧目录分多页（dashboard / 人生历程 / 当前状态 / 徽章成就 等）；角色卡只写 Mochen，不要写祁沫辰、rankai、威动。
- 改造 life-rpg.html：
  - 原 `.wrap` 整体重构为 `.layout`（左侧 `.sidebar` 导航 + 右侧 `.content` 5 个 `.page`）：仪表盘 / 人生历程(journey) / 当前修行(current) / 徽章成就(badges) / 数据&设置(data)。
  - 角色卡 hero：删"祁沫辰"（只留 Mochen）、删"rankai"（改"小门派"）、背景脱敏（删"威动""蓝标""rankai"，改中性叙述"曾于增长一道修行六载…寄身一新兴小门派"）。
  - 新增 showPage(p)：切换 .page.active + .navitem.cur + history.replaceState 同步 hash（刷新/分享不丢当前页）；init 按 location.hash 选首屏页。
  - 响应式：max-width:760px 侧栏变顶部横排。
  - render() 逻辑零改动——所有填充 id 保留（仅重新分组到各 page），静态交叉检查 41 个 getElementById 引用全部命中 HTML，5 page 与 5 nav 一一对应。
- 验证（静态）：SYNTAX_OK；MISSING ids=NONE；contains 祁沫辰/rankai/威动/蓝标 均 false。title/foot 版本号→v5.6。
- 同步 deploy/index.html。待办：用户重新上传覆盖 github 仓库；手机/电脑访问 github.io 即多页版。
- 用户偏好（记此）：life-rpg 角色卡只写"Mochen"，不出现真实中文名；不出现真实公司名（威动/rankai/蓝标等），背景用中性游戏化叙述。

## 页面语义重构 + Todo 融合（v5.7 人生历程版）
- 用户反馈页面语义要更清楚：①「当前修行」= 不同类别当前的状态（四系 live status）；②「人生历程」= 完整人生故事（角色特征/特性/履历/待解锁经历/人生主线/支线）；③ 徽章分已解锁/待解锁；④ 日/周/月/年主线放仪表盘（控制面板），当前修行放状态+行动。并给了「人生 Todo.txt」要求融合。
- 改造 life-rpg.html：
  - 仪表盘(dashboard)：保留 hero+进度+四层概览+每日功课；新增 周主线(#weekBox)/月主线(#monthBox)/年主线(#yearList) 可勾选清单（控制面板定位）。移除原"四大领域即时等级"plain panel（改到当前修行做状态版）。
  - 当前修行(current)：新增「🧭 四系当前状态」(#catStatus)——每系显示 Lv、本周投入(h)、今日 done/total、状态 chip(今日精进/待修/待启)；保留 日课+补剂/周常/羽毛球专精/补录入口。
  - 人生历程(journey)：重构为故事书——🪪角色档案(人物特征/特性/心之所向/生命被点亮)、📜修行履历时间线(启蒙/六载/一程/现世/待解锁×4，done/cur/lock 三态)、🗺️人生主线叙事(#journeyYear 由 S.year 生成只读 arc)、🧩支线叙事(#journeySide 由 week/month 生成只读 arc)、🎯长期里程碑(#goalsBox)、🌟人生愿望清单(由 E:/xwechat.../文本.txt 融合，按 人生/羽琴身/读万卷书行万里路 三组，✔项标 reached)。
  - 徽章成就(badges)：拆分 ✅已解锁(#achsUnlocked) / 🔒待解锁(#achsLocked)，render 里按 a.un 分流。
  - 新增辅助函数：mondayOf/isThisWeek/weeklyAttrMinutes/todayAttrStatus/journeyYearHTML/journeySideHTML。
  - 人设脱敏保持：角色档案无祁沫辰/rankai/威动/蓝标。
- 验证（Node DOM 桩 + 静态交叉）：SYNTAX_OK；44 个 getElementById 引用全部命中 HTML；5 page↔5 nav 一致；祁沫辰/rankai/威动/蓝标=false；render 填 catStatus/journeyYear/journeySide/achsUnlocked/achsLocked 均非空；mondayOf(2026-07-30)=2026-07-27(周一)；isThisWeek 边界正确；标记 CAREER 日课今日完成→todayAttrStatus.done>=1、weeklyAttrMinutes>=30。title/foot 版本→v5.7。
- 同步 deploy/index.html。待办：重新上传覆盖 github.com/mochen61517/rpg；后续想把愿望清单做成可勾选(接状态)可再加。

## 武侠江湖风 + 头像 + 支线任务 + 账本（v5.8）
- 用户需求：头像放上去；整体趣味性/互动效果；偏武侠「侠女闯江湖」；支线小任务分日任务（很快做完）和周任务（需要点时间）；新增个人账本页；确认数据本地不泄露。
- 实现（life-rpg.html）：
  - 头像：读取 `C:/Users/ZhuanZ1/Pictures/头像.jpg`，base64 嵌入 hero 卡，单文件可随身携带。
  - 武侠风：配色改水墨/朱砂/黛青/金黄；字体用思源宋体栈；卡片加卷轴顶线/印章红；按钮加按压阴影；头像 hover 微旋转/放大；升级庆祝改为「境界突破」卷轴。
  - 文案武侠化：「侠女修行录」「每日功法」「周历」「大道」「江湖名帖」「钱庄」。
  - 新增「🎋 江湖轶事」页：
    - 日行一善（日支线，默认 8 项）：给朋友发信息、静坐十分钟、整理书桌、对镜微笑、记开心瞬间、浇花、举手之劳、睡前拉伸。
    - 周游历险（周支线，默认 8 项）：骑车 1h、公园散步 30min、陪父母吃饭、看老电影、手写卡片、探新店、大扫除、读 20 页书。
    - 支持随机抽取、手动添加、删除、完成给 XP、每周一重置周支线；resetSidePool 可恢复默认任务池。
  - 新增「💰 钱庄」账本页：
    - 字段：日期、名称、金额、收入/支出、分类、备注。
    - 展示：本月收入/支出/结余、支出分类条形图、最近 50 条记录。
    - S.ledger 数组存本地，随导出/导入/云备份一起迁移。
  - 隐私说明强化：数据页和 footer 明确「所有数据仅存本机浏览器或本地文件，不上传服务器；只有主动点 GitHub 备份才会经你填写的 Token 上传」。
  - 修复遗留 bug：补定义 `:root` 中缺失的 `--grw`；科技蓝渐变改为金红武侠色。
- 验证：JS 语法检查 OK；JSDOM 桩验证 OK（7 page、render 正常、S 含 sideDaily/sideWeekly/ledger）。
- 版本号：title/foot → v5.8 侠女江湖版。
- 待办：同步 deploy/index.html 并重新上传 GitHub Pages；用户实测头像加载、支线抽取、记账功能。

## 小清新版重构（v5.9）
- 用户需求：徽章要有「升级欲望」描述（例：羽毛球3.0 已达成·正式入门）；愿望清单并入徽章成就页；整体改小清新配色；排版重设计（并行/折叠/放大缩小）；两只猫当灵宠（土豆4岁、Pepper6岁）；进度条修复样式；历史记录移到数据页；写丁火流年大运+当月注意事项；角色档案/历程别只讲事业，加 INTJ/双子座、羽毛球起始年与坚持年数；减少平铺直叙。
- 实现（life-rpg.html）：
  - 主题：v5.8 武侠深色 → v5.9 小清新浅色（奶油/薄荷/淡蓝/柔粉）；追加 `/* ===== v5.9 小清新主题覆盖 ===== */` CSS 块覆盖 :root 变量与各类组件。
  - 进度条修复：`.attr .bf`（原仅 height/width 无背景）→ 补 `linear-gradient(90deg,var(--career),#8edcf0)`；`.qbf` 补背景；`.xpbar/.qbbar/.gbar/.cat-bar` 统一浅轨道+亮填充。
  - 徽章升级化：ach 数组重写带 lv/next 字段（羽球修行 Lv.3.0 已达成·正式入门 / 琴艺修行 Lv.1.0 / 体魄修行 Lv.0.5 进行中…）；`_achCard` 加 Lv 标签 + 下一阶提示 + featured 高亮。
  - 愿望清单：从人生历程页移出，作为「🌟 人生愿望 · 人生副本」面板并入江湖名帖（徽章成就）页，✔ 标 reached。
  - 灵宠：土豆/Pepper 两张图 base64 嵌入角色档案 `.pets-row`（约4岁/约6岁）。
  - 命格·丁火流年面板：新增「🔮 命格 · 丁火流年」（大运/2026流年丙午/12月注意事项折叠）；大运留「待填」占位——需真实出生年月日时才能精确排盘，已提示用户补。
  - 角色档案加：🧬 INTJ·双子座、🏸 羽毛球终身1649h(≈39%)约2017年起坚持约9年；修行履历加「🏸 羽球修行·多年坚持」时间线节点。
  - 布局重排：仪表盘加 `.dash-grid`（12列网格）→ hero-col8 / top-col4 / 周·月-col6 并行；其余 full-width；`<details class="fold">` 折叠命理月历。
  - 历史记录：从江湖名帖页移出，搬到数据&设置页（display:none 默认折叠，点展开）。
- 验证：提取内联 <script> 跑 `node --check` → JS SYNTAX OK；文件完整（2141 行，结尾 </html>）；temp 文件（cat_*.txt / build_v59.py）已清理；deploy/index.html 已同步。
- 待办：重新上传 deploy/index.html 覆盖 github.com/mochen61517/rpg；用户需补「当前大运」真实出生数据（年/月/日/时）才能精确排盘；羽毛球起始年(2017)与累计时数(1649h)为估算，她可在档案里自行订正。

## 真实出生数据 + 江湖轶事重构 + 钱庄导入（v5.10 灵感版）
- 用户补真实出生数据：1995-06-15 上午9:15，北京顺义。
- 八字经万年历/多家排盘核对：乙亥 壬午 **丁丑** 乙巳，日主丁火（初算误为丁酉，已纠正）。大运 乙酉（约 2022–2032，约 7.5 岁起运，依小暑精确时刻推算）。已在「🔮 命格·丁火流年」面板填实（八字/格局/当前大运/大运序列+12月注意），去掉原「待填」占位。
- 羽毛球订正：2021 年起（非2017），终身 **783h**（原1649h，用户给值 782.7999999999993 → 取整783），约5年。档案/履历/成就/const/goal 全改783；删掉 stale「39%」。「等级分不保留小数点」=小时数显整数即可；lvlOf/bmLevel 本就用 Math.floor，等级无小数。
- 江湖轶事重构：
  - 废弃原固定日/周任务数组 → 分类 `sideBank`（45项：29日+15周，8类 社交/创作/美丽/疗愈/灵宠/养生/日常/探索），按画像+时间分配生成（约会男生、录歌发小红书、全妆、穿最爱裙子、80min按摩、康复、头疗、眼护、撸猫10min、脸操、拉伸等）。
  - 加权返场：bank 每项有权重 `w`(0.3–5)；完成/心情好/👍↑w，心情一般般↓w；`sampleFromBank(type,n)` 按权重随机抽。
  - 每日任务每天刷新（sideMeta.dailyDate=todayStr）、每周一刷新（weeklyKey=monday()），**不论完成与否**。
  - 「抽一则」= �红强制（不可删）；其余选择性。新增「💡 补充灵感」入口：用户提想法→bank(cat=灵感,src='user')，重置任务库保留用户灵感。移除旧手动加任务的 attr/min/xp 输入。
  - 函数：toggleSide(item.done+bonusXP+升w)、delSideQuest(拦截强制)、addSideIdea、drawSideQuest、resetSidePool、moodSide/likeSide(调w)、sideListHtml(cat-tag/force-tag/反应按钮)。
- 钱庄导入：用户不每笔记，定期导文档由我汇总分析。加「📥 导入账本文档」面板（file+importLedger 按钮），用 SheetJS(XLSX CDN)。importLedger 读 支出/收入 表（跳过 余额/负债变更），按 date+name+amount+type 去重后入 S.ledger；XLSX 未加载（离线）时优雅提示。
- 已分析用户提供的账本 `C:\Users\ZhuanZ1\Desktop\工作\随手记Mochen账本20260730162243.xlsx`（openpyxl）：2183 支出 / 87 收入，区间 2024-07-30~2026-07-30；总支出 ¥413,873 / 总收入 ¥595,469 / 净 **+¥181,596**。Top 支出：居家物业¥115,708(房租¥85,301)、兴趣爱好¥86,637(羽毛球技能¥23,602+活动¥13,896+体态¥39,163)、饮食¥53,962、学习进修¥47,829、医疗¥36,858。收入：职业¥544,491+其他¥50,978。月度：2025-01 收¥236k(奖金)、2026 Apr/May/Jun 收入0(空窗，与离威动2025-07/蓝标2025-12吻合)。结果存 ledger_summary.json。
- 验证：node --check OK；修一处嵌套模板字符串 bug（qdel 行单引号包 `${}` → 改反引号）；Node vm+DOM/localStorage 桩逻辑测试 ALL PASSED：sideBank 45/8类、日4周3刷新、抽强制、完成+8 bonusXP、加灵感→灵感类、重置保留用户灵感、采样4/3、importLedger 存在。
- 同步 deploy/index.html。temp：patch_v510.py / vmtest_v510.js / smoke_v510.js / analyze_ledger.py / _check.js 为构建校验脚本（可留档或清）。
- 待办：重新上传 deploy/index.html 覆盖 github 仓库；用户实测八字面板/江湖轶事抽取返场/钱庄导入（联网首次加载 SheetJS）。

## v5.11（仪表盘重构 / 2026-07-30 18:00）
- 用户反馈「当前修行」页面顶部混入 dashboard 信息（年主线 checklist 错乱显示），要求重构。
- 导航顺序改为：仪表盘 → 短期任务 → 长期任务 → 江湖轶事 → 钱庄 → 角色设定 → 徽章成就 → 数据&设置。
- **仪表盘重构为总览入口**：
  - 顶部角色卡（点击→角色设定）+ 钱庄卡（点击→钱庄，显示本月收/支/结余）。
  - 总等级+灯火不熄+历史继承/权重说明。
  - 已点亮徽章摘要（点击→徽章页）。
  - 今日任务 / 本周任务 / 江湖轶事 摘要卡片（点击分别进入短期任务/短期任务/江湖轶事）。
  - 本月主线 / 今年大道 摘要卡片（点击进入长期任务）。
  - 底部保留「四系当前状态」总览。
- **当前修行 → 短期任务页**：只保留 recbar、日功、补剂、周历、羽毛球专精；移除四系状态等 dashboard 信息。
- **新增长期任务页**：
  - 十年周期规划（2026–2035）：2026「安身立命」进行中，2027「伴侣+结婚」、2028–2035 均为「待开启」。
  - 2026 每月主线（1–12 月具体规划）。
  - 今年大道 checklist（从仪表盘移入）。
- **人生历程 → 角色设定页**：标题/导航/psub 改为「初始设置 · 成长历程 · 成长方向 · 灵宠 · 丁火流年」。
- 修复 v5.10 遗留 bug：`<style>` 块未正确关闭（缺 `</style>`），导致样式泄漏；patch 时补上 `</style>` 并插入新 CSS。
- 修复 patch 中误截断的 dashboard 头像 base64：从 deploy/index.html(v5.10) 还原完整 2.8MB 头像数据。
- 验证：node --check OK；Node vm + DOM/localStorage 桩逻辑测试 ALL PASSED：仪表盘摘要、四系状态、短期任务、长期任务（十年/月规划/年目标）、角色设定、江湖轶事、钱庄均正常渲染。
- 同步 deploy/index.html。
- 待办：重新上传 deploy/index.html 到 github.com/mochen61517/rpg；用户实测各页切换与点击入口。

## v5.11.1（版块内容隔离 / 2026-07-30 18:10）
- 用户截图反馈仪表盘左侧导航下方出现「丁火 · 喜金水清凉 / INTJ · 双子座」，认为各版块内容混乱。
- 修复：将 sidebar 全局 `.side-foot` 移除，改为只在「角色设定」页底部显示。
- 修复：将仪表盘中的「历史继承底分说明」「权重说明」两个长段 hint 移到「角色设定」页的「修为说明」面板内；仪表盘只保留总等级/XP/灯火摘要。
- 这样每个版块确实只显示自己的内容：仪表盘是摘要入口，角色设定是角色相关详情。
- 验证：node --check OK；结构检查确认 `.side-foot` 与 `histInherit/weightHint` 仅在 `page-journey` 内。
- 同步 deploy/index.html。

## v5.11.2（江湖轶事/钱庄/消费纪律 / 2026-07-30 18:15）
- 用户反馈：江湖轶事页顶部仍有总览感；羽毛球 783h 旁「约 5 年」时间描述不对；钱庄数据空；dashboard 钱庄应只展示当前家产+今年利润；钱庄详情需账户/当月/今年收支分类/优化建议；随机任务应体现消费纪律（今日不许叫外卖、本周不许打车、本月不许买衣服）。
- 修复：移除江湖轶事页 hero panel，直接显示任务操作与列表。
- 修复：羽毛球文案统一为「2021 年起」，去掉有争议的年限描述。
- 修复：仪表盘钱庄卡片改为「当前家产 + 今年利润」；无数据时显示空状态提示。
- 增强钱庄详情页：新增「当前家产」「各账户情况」「今年收入分类」「今年支出分类」「优化建议」面板；保留「当月账簿」与最近记录。
- importLedger 解析时额外读取「支出账户/收入账户/账户」字段并保存到 ledger.account。
- 江湖轶事增加月任务周期：sideMonthly、monthlyKey、每月 1 日刷新、支持抽取/完成/删除/心情/喜欢。
- 随机任务库新增「纪律」分类与消费纪律任务：日任务「今日不许叫外卖」、周任务「本周不许打车」、月任务「本月不许买衣服/新包/新鞋」等。
- 顺手修复 newDay() 中周游历险重置逻辑被放在 lastWeekly 更新之后的 bug，合并到同一条件内。
- 验证：node --check OK；VM 桩测试全部通过（月任务生成/完成/抽取、纪律任务存在、钱庄新元素存在）。
- 同步 deploy/index.html。
- 待办：用户实测导入 xlsx 后钱庄分类与建议是否准确；重新上传 deploy/index.html 到 github。

## v5.11.3（页面切换修复 / 2026-07-30 18:28）
- 用户截图反馈：点击「短期任务」后，主区域仍显示仪表盘内容（顶部仍是概览）。
- 根因：`.dash-grid{display:grid}` 与 `.page{display:none}` 选择器优先级相同，但 `.dash-grid` 在 CSS 中靠后，导致仪表盘即使失去 `.active` 也保持 `display:grid`。
- 修复：改为 `.page.active.dash-grid{display:grid}`，仅在仪表盘激活时生效。
- 顺手修复：总等级 XP 显示出现 `1030.3000000000001` 浮点，将 `xpInLvl()` 返回值取整（xp/pct 均 Math.floor）。
- 验证：node --check OK；CSS 选择器已修正；deploy/index.html 已同步。
- 二次静态校验（"继续"轮）：grep 确认 `.page.active.dash-grid` 生效、独立 `.dash-grid{display:grid}` 已消除；8 个 page id（dashboard/current/longterm/side/ledger/journey/badges/data）与全部 showPage 导航目标一一对应，无其他 display 规则会在非激活时泄露。页面隔离确认干净。
- 待办：用户实测各导航切换；重新上传 deploy/index.html 到 github。

## life-rpg v5.11.4：dashboard 排版 + 羽毛球累计时间修正
- 用户反馈 dashboard 排版乱，要求高度相近的版块并排放；同时指出羽毛球终身累计应为 1649h（非 783h）。
- dashboard 卡片重新分组：
  - 第一行：角色卡 col8 + 总等级/灯火 col4
  - 第二行：钱庄 col6 + 已点亮徽章 col6
  - 第三行：今日任务 col4 + 本周任务 col4 + 江湖轶事 col4
  - 第四行：本月主线 col6 + 今年大道 col6
  - 第五行：四系当前状态 col12
- 羽毛球终身累计统一改为 1649h：常量 `BADMINTON_LIFETIME_HOURS`、历史继承 `HISTORY_HOURS.BADMINTON`、长期目标羽毛球 `cur`、角色设定/履历/短期任务页/徽章描述中的文案与数字。
- 同步 deploy/index.html；node --check 通过。
- 版本号后续可标 v5.11.4。

## life-rpg v5.11.5：dashboard 按行分组布局
- 用户反馈 v5.11.4 dashboard 同一行卡片高度相差太多。
- 根因：CSS Grid 单行内卡片高度由内容决定，高卡片与矮卡片硬排导致空白不均衡。
- 解决：引入 `.dash-row` 行容器，每行独立 grid，卡片严格按内容高度分组：
  - 第1行：角色卡 col8 + 总等级/灯火 col4（内容较多）
  - 第2行：今日任务 col4 + 本周任务 col4 + 江湖轶事 col4（任务列表，高度相近）
  - 第3行：钱庄 col6 + 已点亮徽章 col6（内容较少）
  - 第4行：本月主线 col6 + 今年大道 col6（内容较少）
  - 第5行：四系当前状态 col12（全宽）
- CSS 调整：`.page.active.dash-grid{display:block}`，新增 `.dash-row` 12 列 grid，`align-items:start`，卡片 `align-self:start` 防止拉伸。
- 同步 deploy/index.html；node --check 通过。
- 版本号后续可标 v5.11.5。

## life-rpg v5.11.6：dashboard 卡片可拖拽排序
- 用户要求 dashboard 做成可自己拖动的，避免反复调排版。
- 实现：去掉 `.dash-row` 分组，改回统一 12 列 grid；每张卡片加 `data-dash-id`；标题栏加「🔧 拖拽排序」开关 + 「↺ 重置」按钮。
- 拖拽逻辑：`initDashDrag()`（dragstart/dragover/drop 交换 DOM 顺序）、`saveDashOrder()`（存 localStorage 键 `lifeRPG.dashOrder`）、`applyDashOrder()`（加载时按保存顺序重排）、`toggleDashSort()`（排序模式下卡片 draggable=true 且临时移除 onclick 防误跳转，关闭时恢复并保存）、`resetDashOrder()`（清键并 reload）。
- CSS：`.dash-grid.sorting .panel` 显示虚线 outline + grab 光标；`.dragging` 半透明、`.drag-over` 高亮。
- 验证：node --check 通过；Node VM + DOM 桩跑四个核心函数无运行时错误，顺序保存正确。resetDashOrder 在桩环境报 location 未定义属正常（浏览器有 location）。
- 版本号 v5.11.6；deploy/index.html 已同步。

## life-rpg v5.11.7：总等级长条 + 钱庄历史基线
- 用户两需求：① 总等级版块做成全宽长条，放 Mochen 角色卡下；② 钱庄为什么没数据（之前上传过账本）。
- 需求1：总等级卡片 class 从 `col4` 改 `col12 lvl-bar`，内部改横向 flex（等级+进度条+经验+灯火+升级特权一行）；CSS 加 `.lvl-bar` 系列。角色卡全宽(col12 默认)，总等级 col12 自然在角色卡正下方。
- 需求2 诊断：钱庄读浏览器 localStorage 的 S.ledger，当前为空。原因二选一：① 之前上传账本在另一文件/origin，localStorage 不互通；② 上传时 SheetJS(CDN) 未联网加载致导入被拦。本地已无原始 xlsx（仅 v5.10 分析的 ledger_summary.json 年度汇总，非逐笔）。
- 解决：把 ledger_summary.json 关键数据嵌入为常量 LEDGER_BASELINE（net 181596、收入595469、支出413873、支出分类、收入分类、按年收支），在 S.ledger 为空时回退展示：
  - 仪表盘钱庄卡：显示当前家产(基线)+今年利润(基线)+「历史基线 2024-07-30~2026-07-30（非逐笔）」提示。
  - 钱庄汇总页：抽 renderLedgerBaseline(year) 函数，ledgerHtml 开头无逐笔时调用并 return，填充家产/今年收支分类/优化建议/「无逐笔明细」提示。
- 验证：node --check 通过；Node VM + DOM 桩跑 renderLedgerBaseline('2026') 正常，家产/分类/建议均生成。
- 版本号 v5.11.7；deploy/index.html 已同步。
- 注意：基线为汇总数据，无逐笔/当月/账户明细。用户若想完整，需重新找到原 xlsx 导入（需联网加载 SheetJS）。

## life-rpg v5.11.8：真实逐笔账本接入（用户指正"有逐笔交易"）
- 用户纠正：账本是带逐笔明细的，源文件 `C:/Users/ZhuanZ1/Desktop/工作/随手记Mochen账本20260730162243.xlsx`（此前发给过）。之前 v5.11.7 用 ledger_summary.json 年度汇总是非逐笔基线，属误判。
- 解析：openpyxl 读 支出(2183)+收入(87)=2270 条；余额变更/负债变更两表为转账，按 app 既有规则排除。字段映射：name=二级分类||一级分类||项目，cat=一级分类，account=支出/收入账户，note=商家+备注，date=YYYY-MM-DD。
- 校验：收入 595469 / 支出 413873 / 净额 181596，与 v5.10 基线逐项吻合 → 确认同源真实数据。
- 新增离线导入：app 加「导入账本(JSON)」按钮 + `importLedgerJSON()`，读 ledger_import.json 按 id 去重合并进 S.ledger，不依赖 SheetJS/CDN。去重 key 用每条唯一 id（导出时 L0000001.. 序号），避免"同日同额多笔"被误删（初版用 date+name+amount+type+account+note 误删 12 条；改用 ts 因源日期是文本导致 ts 全同又误删；最终用唯一 id 最稳）。
- 生成物：`E:/.workbuddy/2026-07-29-14-27-18/ledger_import.json`（2270 条，id 唯一）。
- 验证：node --check 通过；Node VM 实测 importLedgerJSON：空账本导入 2270 条、金额吻合；预置同 id 重复精确跳过 1 条。
- 版本 v5.11.8；deploy/index.html 已同步。用户操作步骤：钱庄页 →「导入账本(JSON·离线)」→ 选 ledger_import.json → 钱庄即显示真实逐笔账本（基线仅空数据时兜底）。
