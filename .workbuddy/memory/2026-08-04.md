# 2026-08-04

## 人生RPG追踪器 v5.16.0（life-rpg.html）
Mochen 选定三项新增功能并全部实现、本地提交 deploy（commit 2c92792，push 仍被沙箱拦截，需她本机 `git push origin main`）。

1. **命理主题皮肤**：重构为 `data-theme` 三主题系统——小清新(默认/原貌保留) / 丁火·清凉(bing，青蓝水色+水墨衬线，贴合丁火喜金水) / 命理·夜(dark，暗色 serif)。设置页加切换按钮，持久化到 `S.theme`，启动 `applyTheme()` 应用。原亮色块保持 always-on 不变，新主题用 `html.theme-X` 更高优先级覆盖变量+关键元素规则，避免污染旧主题。
2. **趋势曲线**：`S.trend` 时序快照，每次 `save()`（含 newDay）自动记 {d, xp, net, w}；仪表盘新增「📈 修行趋势」卡片，SVG 折线展示 经验/净资产/体重（净资产需先录资产快照、体重用新增「⚖️ 身体指标」录入）。同日期只更新不追加。
3. **周报/月报 + 微信推送**：设置页「📊 周报/月报」面板，生成汇总（完成行动/四系投入时长/月年主线/新到访/嘉奖掉落/突破/等级），`genReport(kind, push)` 复用 pushplus 推送。纯静态页无法后台自动推送，已在提示里说明可加 WorkBuddy 定时提醒。

## 测试与踩坑
- node --check 通过；用 DOM/函数桩做的 18 项逻辑测试全 PASS。
- 两处真实 bug 已修：① buildReport 内 `const yearDone` 与全局 `yearDone()` 函数同名遮蔽→改名为 `yearDoneList`；② 嘉奖掉落/突破按 ts(含时分) 与裸日期 end 做字符串比较会丢失「当日」记录→改为 `ts.slice(0,10)` 按日期比较。
- 注意：trend 需跨多日才有曲线（同日期不追加），首数日会显示「数据不足」提示，符合预期。

## 后续：游戏化/粘性机制清单（已给建议，待 Mochen 选择）
现有导航页：仪表盘 / 短期任务 / 长期任务 / 江湖轶事 / 钱庄 / 角色设定 / 徽章成就 / 旅行地图 / 战利品 / 数据设置。已有机制：徽章成就、装备加成、嘉奖掉落、连击(灯火不熄)、地图点亮、四系等级、愿望、主题、趋势、周月报；灵宠与丁火流年仍是占位未落地。
建议清单按层分：①反馈层（勾选即时 +XP 飘字/升级特写/每日战报结算页）②节奏层（补签护身符防断链、每周限时挑战、精力条）③叙事层（章节·卷制、灵宠落地、丁火流年落地、NPC 委托）④长期层（四系技能树、赛季制+称号）⑤外部（战报分享图、球友对战记录）⑥意外性（每日抽签影响掉落率、稀有奇遇事件）。
推荐起步组合：反馈动效 + 补签护身符 + 每日抽签（成本低、直接影响每日体感与留存）。

## 人生RPG v5.17.0（游戏化九件套，已交付，commit f962b0a）
基于上面清单，Mochen 选定：反馈三件套(全) + 精力条(节奏层只做此) + 章卷制/丁火流年/NPC委托(叙事层做1/3/4，灵宠未做) + 四系技能树/赛季称号(长期层全做)。

实现要点（life-rpg.html，块起始 `// ===== v5.17 精力条`，~4159–4642 行）：
- **反馈三件套**：勾选飘字 `floatXP` 与升级特写 `celebrate` 复用既有逻辑；新增「今日战报」`buildBrief/showBrief/maybeShowBrief`——新开页面/次日首次进入，若昨日有活动则弹「昨日战报」(完成数/投入时长/经验增量 trend 差/掉落/连击/精力/丁火建议)，关闭后写 `S.brief.last` 防重复。
- **精力条 `energyState()`**：近3日投入(540分钟≈满负荷)→负荷；睡眠+18/拉伸+8/近7日疗愈+14/今日零投入+10→恢复；`v=clamp(100-load+rest)`；分充盈/平稳/偏低/透支四态，过载劝休。
- **章卷制**：4 卷(寻锚/立基/云水/自在)，`curVolume()` 按年道主线完成度定位，`checkVolume()` 达成写 `S.saga.done`+掉落，幂等不重复入账；`chapterText()` 渲染当前卷剧情。
- **丁火流年**：`JIEQI` 24 节气表 + `WUXING_ADVICE` + `dingHuoNow()` 给当期五行旺衰与作息建议；`jieqiSuggest()` 按五行出 3 条当日可加任务。
- **NPC 委托**：`NPCS` 4 位(林教头/沈掌柜/云娘/白鹭先生)，`npcRoll()` 每周一刷新四方委托，交差 +30XP，四人全清额外掉落。
- **四系技能树 `SKILL_TREE`**：四系各 6 节点(有前置/成本/加成)，修行点=`lvlOf-1`，`skillBonusFor(attr)` 加成叠加进 `grant()`：`eff=xp*(1+equipBonusFor+skillBonusFor)`。
- **赛季称号**：`curSeasonKey=YYYY-Qn`，`seasonCheck()` 季度切换自动结算、`TITLES` 8 称号按赛季统计 `seasonStats` 条件发放，`wearTitle` 佩戴至头像昵称。

验证：`_v517_test.js`（DOM/函数桩，73 项断言）全 PASS。过程中发现 **1 个测试桩 bug 非产品 bug**：战报 `dxp` 实际取 `S.trend` 快照差（需 ≤dateStr 至少 2 条才非 0），原测试误以为按任务 XP 求和、且 fixture 只给了 1 条 ≤Y 的 trend → 改 fixture 补 `sd(-2)` 快照后 dxp=120 符合预期。产品逻辑本身正确。

发布：deploy/index.html 已 `cp` 并 commit f962b0a。沙箱仍不能 git push，需她本机 `git push origin main`（或网页上传 + Ctrl+F5 强刷）。版本号已升 v5.17.0（title + brand-t 两处）。临时测试文件 `_v517_test.js`/`_chk.js` 已清理。

## 人生RPG v5.18.0（今日一句激励横幅，已交付，commit 74027cb）
Mochen 要 dashboard 顶部每天一段话激励她，来源可选名著/名言/影视/歌词，并可对应当天节气。

实现（life-rpg.html，块起始 `// ===== v5.18 今日一句`，~4652 行）：
- **位置**：dashboard 顶部第一个卡片 `#quoteBanner`，带 `panel` 但不带 `data-dash-id` → 固定置顶、不参与拖拽排序。
- **语料 `QUOTES`**：36 条（28 通用 + 8 节气专属）。通用含《山月记》"本非美玉"、《小王子》、《德米安》"找到自我"、《悉达多》、罗曼·罗兰英雄主义、鲁迅、加缪西西弗、毛姆、肖申克、阿甘、功夫熊猫、当幸福来敲门、李宗盛《山丘》《给自己的歌》、朴树《平凡之路》、五月天《倔强》、毛不易《消愁》、陈奕迅《富士山下》、陶渊明、苏轼《定风波》、李白、论语"知者乐水"、海上钢琴师、凡人歌等——多选贴合她长期主义/自我怀疑/自由价值/丁火喜水气质。
- **节气专属**：雪莱《西风颂》(冬春)、增广贤文(春)、寻梦环游记(清明/寒露/霜降)、诫子书(夏火需静)、后汉书(秋)、中庸(芒种)、警世贤文(冬)、林则徐(冬)。每个 24 节气至少命中 1 条专属。
- **取句 `dailyQuote()`**：优先当令节气专属池，否则通用池；用 `todayStr()` 哈希做种子 → 同日确定性、隔日轮换。
- **渲染 `renderQuote()`**：引号大字 + 衬线正文 + "—— 出处 · 当令 节气名"（仅当令专属时显示节气标）。已接入 `render()` 的 try 块。

验证：`_v518_test.js`（57 项断言）全 PASS——库完整、24 节气全有当令专属、同日确定性、render 写 DOM 含出处与当令标。node --check 通过。发布 commit 74027cb；沙箱仍不能 push。临时文件已清理。

## 人生RPG v5.18.1（今日一句扩容 + 换一句，已交付，commit dee198d）
Mochen 反馈 v5.18.0 的 28 条通用太少。扩容至 **107 条**（99 通用 + 8 节气专属），并加「换一句」按钮解决"用完一批更新"诉求。

实现改动（同 `// ===== v5.18` 块）：
- **语料扩容**：通用从 28 → 99。新增分片：中国古典（庄子/老子/王维/辛弃疾/杜甫/曹操/屈原/王国维/王阳明/曾国藩/格言联璧）、世界文学（尼采/里尔克/苏格拉底/亚里士多德/康德/罗素/王尔德/海明威/梭罗/卡夫卡/村上/芥川/菲茨杰拉德/伍尔夫/萧伯纳）、影视（一代宗师/无问西东/心灵奇旅/楚门/死亡诗社/三傻/绿皮书/实习生/白日梦想家/放牛班的春天/爆裂鼓手/摔跤吧）、歌词（许巍蓝莲花/逃跑计划/郑钧私奔/郝云活着/万晓利/刺猬）、英文歌词（Let It Be/Here Comes The Sun/Don't Stop Believin'/The Climb/Imagine）、现代名言（爱因斯坦/乔布斯/巴菲特/芒格/丘吉尔/罗斯福/培根/富兰克林）、心理成长（武志红/陈海贤《了不起的我》/托利/萨特）。节气专属 8 条不变。
- **「换一句」按钮 `.q-shuffle`**：`dailyQuote(off)` 新增偏移参数；`_qoff` 同日偏移经 `localStorage('qoff_'+todayStr())` 持久化（刷新不丢，隔日自动归零）。逻辑：offset=0 基线优先当令（带「当令」标）；当令池 ≥2 条时换一句在当令池内轮换；当令池 ≤1 条时落入通库，保证「换一句」始终有变化（避免出现只有 1 条当令时按钮"失效"的体验坑）。
- CSS 加 `.q-shuffle` 金色描边小圆钮样式。

验证：`_v518b_test.js`（21 项断言）全 PASS——库≥90/通用≥80/节气=8、无重复文本、同日确定性、当令分支正确、24 节气全不崩、shuffle 增量+持久化+产生不同句。node --check 通过。发布 commit dee198d；沙箱仍不能 push。临时文件已清理。

## 人生RPG v5.19.0（互动版块 4 件套，已交付，commit 89e2a41）
Mochen 喜欢 NPC 四方委托的互动感，指定把"其他版块也这样设计"。挑了菜单里的 ②③④⑤ 实现：**② 每日宜忌抽签 / ③ 远方来信 / ④ 江湖偶遇 / ⑤ 成就羁绊**。复用同一套 NPC 阵容（林教头/沈掌柜/云娘/白鹭先生）。

实现（新增 `// ===== v5.19` 块，~4869–5133 行）：
- **② 每日宜忌抽签**：`YIJI` 16 条宜忌 + `YIJI_REST`。`drawRoll()` 每日首次渲染时按 `energyState().state==='透支'` 强制"宜休息"，否则随机；落 `S.draw` 同日确定性。`drawClaim()` 领 +20 XP（幂等）。仪表盘新增 `#drawBox` 卡片（data-dash-id=draw，可拖拽排序）。
- **③ 远方来信**：`LETTERS_ORDER`(云南置首) + `LETTERS`(10 地 16 封，云南 3 封、其余 1~2 封，部分带小任务)。`letterCheck()` 每天至多解锁 1 封（优先未读时不补），落 `S.letters.unlocked`；`openLetter` 弹 modal 阅读置已读，`respondLetter` 领任务 XP。旅行地图页新增 `#letterBox` 面板 + `#letterModal`。云南信件直接呼应她"想去云南/丁火喜水"愿望。
- **④ 江湖偶遇**：`ENCOUNTERS` 6 则（4 NPC + 卖花老太太 + 茶摊老板），`encWeight` 按当日该系投入加权（近期有行动更易撞见对应 NPC）。`encounterRoll` 加权随机落 `S.enc.cur`，`encPick` 选支线给 XP/记录/清场。江湖轶事页新增 `#encBox` + 「🪄 探寻江湖」按钮（也由 init/newDay 自动置一则可遇）。
- **⑤ 成就羁绊**：`BONDS` 5 系（强健/羽道/灵台/立基/云水），按现有徽章 id 分组。`bondDone` 全亮即达成 → `grantBond` 自动授称号（入 `S.season.titles`）+ 记 `S.bonds.awarded`；`openBond` 弹 NPC 评语，`shareBond` 用 canvas 生成"江湖名帖"PNG 下载。徽章成就页新增 `#bondBox` + `#bondModal`。

接入：defaultState/migrate 加 draw/letters/enc/bonds 四字段兜底；render() 加 `renderDraw/renderLetters/renderEncounter/renderBonds`；newDay 与 init 加 `letterCheck()` + 自动 `encounterRoll(true)`；通用 `showModal/hideModal`。加 v5.19 CSS 块。版本升 v5.19.0（title + brand-t）。

验证：`_v519_test.js`（26 项断言）全 PASS——抽签(含透支强制/幂等)、来信(解锁节奏/已读/任务XP)、偶遇(roll/clear/XP)、羁绊(done判定/授称号幂等/auto-grant)。node --check 通过。发布 commit 89e2a41；沙箱仍不能 push。临时文件已清理。
